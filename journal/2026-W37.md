# Week 37, 2026

## 2026-09-07 (Mon) — readers finish the job: the typed lane stops being silent

Ross picked the "readers finish the job" unit off the list ("convinced
me - silent dropping scary..."). The reproduction on one fixture — 1001
int rows then `1002,1.5` — was worse than the TODO entry said. exec CSV:
`Error: row 1002 … "1.5" is not int`, exit 1 (v4.88.0). Typed CSV: **1002
rows, sum 501501, exit 0** — the row kept, the cell zeroed (the exact
signal-became-zeros shape DFC124 fixed in exec, alive in the compiled
lane). Typed JSONL: 1001 rows, exit 0 — the row dropped. And a missing
input file: an empty table, exit 0, from every typed reader. My own
path mistake found that last one (the program ran in a subdirectory the
fixture wasn't in, and printed a header with no rows).

The fix has one shape across `typed/`: the lossy readers panic with a
`*typed.ReadError` (Op, Source, Row or Line, Err) — the root package's
`ssql.ReadCSV` / `*CellError` contract, mirrored — and the `*Safe`
readers yield it. `rowSchema.decode` and the JSONL plan now say
`column "v": "1.5" is not int64` (the strconv text was useless to a
user). The part that needed thought: a panic inside a shard goroutine
does not reach the generated program's `recover` in main — the process
dies with a goroutine trace, exit 2. So `shardGroup` (read_error.go)
runs every fan-out consumer's workers (`Serial`, `SerialCount`,
`GroupByParallel`, the CSV/Delim/Parquet per-shard sinks, top-k,
`DistinctParallel`, `Parallel`'s feeder) and re-raises the first panic in
the waiting goroutine; `Serial` rethrows after its fan-in channel
closes. `TestStreamConsumersPropagateShardPanic` drives each consumer
with the parallel CSV reader on the late fixture. Race clean.

Then the remedy had to exist. The exec error says "override with `-type
v float`" — and typed codegen REFUSED `-type` ("column overrides not
supported in typed mode"), `from tsv` and `from jsonl` had no such flag
at all, and the `-sample` / `-last` record fragments dropped the
overrides they were given. Now `lib.TypeOptions` flows into all three
samplers (an overridden column takes the named Go type; a `_schema`
header is re-rendered with it), `from tsv` grew `-type` / `-default-type`
(a `typeArgs` bundle threads flags + resolved `CSVConfig` through
exec/sample/last/codegen), and `from jsonl -type` is a strict per-record
coercion, `ssql.CoerceFieldTypes` — JSON has one number type, so a
whole float into int is exact and `1.5` into int is a `CellError`
("explicit column type" — `recoverCellError` words the hint
differently). `TestTypeOverrideMakesEveryLaneAgree`: exec, record and
typed × csv/tsv/jsonl all give 1002 rows / 501502.5.

TSV was the third item: `parseTSVValue` typed each value on its own, so
it never erred — it produced a mixed column, and `""` for an empty
cell. `readCSVRows` split into `readRows(rowReader, …)` and
`tsvRowReader` (header fixes the separator, no quoting, blank lines
skipped, row slice reused — readRows clones what it keeps for the
sample) is the second implementation of that interface. One typing
implementation for both formats. `int_first_tsv_floats_survive` joins
the equivalence corpus.

The union/merge side-file item (TODO 2026-09-04) turned out to be
bigger than "use the schema-aware reader": the first codegen test of a
regular-file union or merge found neither template had ever compiled.
Union's generated code referenced `ctx.Stderr()`; merge inlined the
`os.Open` + read into a stmt fragment, which the assembler treats as a
filter expression (`extractFilter` strips the first `X :=` and applies
`(records)`) — the sabotage run with the v4.90.0 binary died at `go
build`. Merge now emits an init fragment per side file plus a filter
closure, union's shape. Noted the sibling: join's record right-side
reader does `if err != nil { return nil }` — a missing right file is an
empty join with exit 0. In TODO.

Lessons for the file: (1) "fails loudly in exec" says nothing about the
compiled lanes — the equivalence gate compares OUTPUTS of pipelines
that succeed; a lane that exits 0 with a different row count only
diverges if a case's data has a late mismatch, and none did. The
readers_loud tests assert exit status and message per lane, which the
equivalence normaliser cannot. (2) Whenever an error message names a
remedy, the remedy needs a test in every lane the error can appear in —
the `-type` advice was three-lanes-wide and one-lane-true. (3) A panic
in a worker goroutine is not "loud" — it is a crash; recover has to be
where the caller can see it.

`limit 0` thread: Ross explained the dial use (journal W33) and asked
about a bare `limit` meaning "no limit". My view, given: prefer bare
`limit` as the dial and `limit 0` as SQL's zero rows (header-only reads,
DuckDB agrees); changing it is a CHANGELOG-worthy semantics change.
Untouched pending his call.

Gates, all green: typed (+race), root, lib, equivalence (new tsv +
jsonl-override cases), the readers_loud lanes, full cmd/ssql suite
(573 s), scale gate, doc-check, doc-test. Uncommitted at end of entry.

## 2026-09-07 (Mon) — `ssql codelab`: the tutorial no longer asks for a clone

Ross spotted it reading the codelab as a newcomer would: the setup block
said `git clone https://github.com/rosscartlidge/ssql.git` to reach
`doc/codelab-data/` — a multi-gigabyte repository (history plus baked
debs and wasm) for 44 KB of CSV. Nobody on the learning path should
need the source tree; `go install` already delivers the binary, so the
binary now delivers the data too.

Built: `doc/codelab-data/embed.go` (package `codelabdata`) embeds the
fixture files in place — the checked-in files ARE the embedded ones, so
the runner, the tests and the shipped binary read the same bytes with
no sync step (contrast the wasm/ssql-ui.js copy that needs a drift
test). `codelabdata.Write(dir, force)` creates the directory, refuses
with a list of the clashing names unless `-force`, and returns the
paths written. `ssql codelab [DIR]` (`cmd/ssql/commands/codelab.go`,
default `ssql-codelab`, `-force`) prints the paths and the next three
lines a reader should type. Registered in both mains (the drift test
insists; harmless in the playground). `scripts/codelab-run.sh` stages
its throwaway copy through `ssql codelab "$WORK"` instead of `cp -r`,
so `TestCodelabRuns`/`make doc-test` now prove the setup step as well
as the examples. Codelab setup rewritten (install → `ssql codelab` →
`cd ssql-codelab`); README learning path, codelab-data README, DFC125
(amendment under decision 1), TODO, CHANGELOG updated.

Why embed from `doc/` rather than move the data under `cmd/`: every
inbound reference (DFC125, runner, optimise tests, README) points at
`doc/codelab-data/`, and a Go file in a data directory costs nothing —
Go import paths may contain a hyphen, the package clause is
`codelabdata`. Gates: commands unit test (byte equality with the
checked-in files, refuse-then-force), registration drift test,
codelab runner 39/0/6 and signal codelab 34/0/13, js/wasm slim build,
doc-check 37/37. Uncommitted, alongside the readers unit above.

Follow-up the same afternoon, again from Ross reading as a newcomer:
"Go 1.23+" and a bare `go install` are not instructions. The setup now
gives the `apt-get install -y golang-go` line, and explains the fact
that makes it sufficient: go.mod says `go 1.26`, and any Go ≥ 1.21
honours the directive by downloading that toolchain on the first
`go install` (verified here with the Ubuntu 24.04 package,
`/usr/lib/go-1.22/bin/go`, in a clean HOME — 54 s to a working
`ssql version`). It also says where `go install` puts the binary
(`$HOME/go/bin`, not on PATH by default) and gives the exact
`~/.bashrc` line. Same facts propagated to README's Quick Start and
Option 2, api-reference's Requirements/Install Go (which still told
people to wget go1.23.0), and the signal codelab's install block.
Every "Go 1.23+" that meant *required to install* was wrong on two
counts — the module is 1.26, and the real floor is 1.21 — the ones
that describe the iterator feature stay.

Then the bar Ross actually set: "precise enough that a novice can start
running the codelab with completion and tmux". Setup is now six named
steps in the order a fresh machine needs them — install Go, PATH,
`ssql codelab` + `cd`, install and start tmux *in that directory* (what
tmux is in one sentence, the 3.2 popup floor, the green bar as the
"you are inside" signal, `exit` to leave), the `eval` line with the
bash caveat and the one-line `~/.bashrc` append, then the four try-it
lines with the keys spelled out (Tab; Alt-h and the Esc-h / macOS
Option-as-Meta fallback; Alt-H for the key list). The data step was
moved ahead of tmux so the field/value completions work the first time
they are tried. Runner: 39 pass, 10 skipped (every skip is a
run-once-by-hand install step), doc-check clean.

Ross caught a wrong key in the try-it block: it told the reader to Tab
for field names and values across a pipe. That has been Ctrl-O since
v4.50.0 (W26 — bash Tab cannot see past the last `|`; it inserts a
`Use-Ctrl-O` / `Values-Use-Ctrl-O` reminder, confirmed with
`ssql -complete 3 where -if ""`). Tab does do fields and values in the
`ssql serve` console, which owns the whole line — the serve codelab's
sentence was right, the bash codelab's was not. Setup now says which key
does what, why there are two, and what happens if you press the wrong
one; README's walkthrough link pointed at a heading (`#interactive-shell`)
that no longer exists and now points at Setup.


## 2026-09-07 (Mon) — v4.91.0 released

Ross: "cut a release." Contents: the readers unit (typed readers fail
fast with `typed.ReadError`, `-type` honoured in every lane and grown on
`from tsv`/`from jsonl`, TSV columns typed like CSV, union/merge side
files compiling for the first time), `ssql codelab` with the embedded
fixtures, and the novice-proof Setup (apt-get Go, the toolchain fact,
`$HOME/go/bin`, tmux, the eval line, Ctrl-O not Tab for fields and
values).

Gates before tagging: race (typed), root/lib/commands, scale (14 s),
permutation triples (185 s), explore e2e, doc-check, and the full
cmd/ssql suite. The full suite ran AFTER the version bump, so every
`generate go -run` case (pipeline/script flags, run keybinding, serve
typed head, codelab runner, from-ssh typed re-entry) failed on
`unknown revision v4.91.0` — expected, the module did not exist yet;
those are re-run post-tag. One real failure: `TestServeConsoleRegistration`
wanted `Codelab` either in the SSH console or excluded with a reason —
excluded (writes local files; a CLI setup step). Lesson for the
sequence: bump version AFTER the full suite, or read its failures with
that in mind.

Sequence as always: chore commit (changelog, version, wasm refresh,
Ctrl-O fix) → bake commit.txt → annotated tag → push --follow-tags →
debs baked and committed → install-local both v4.91.0 (gpu: no / yes)
→ WebVM 386 slim build pushed to ssql-terminal + Deploy dispatched →
playground workflow green. Proxy verify: sum.golang.org answered 404
"unknown revision v4.91.0" for ~15 minutes after the push (same class
as W26's v4.50.0 and W33's v4.60.0: the first lookup ran seconds after
the tag push and the miss was negative-cached) — kept retrying rather
than `GOSUMDB=off`; cleared on the 15th one-minute retry. Ross saw
"unknown revision" on his own direct install meanwhile and asked
whether to push a higher version — no: `git ls-remote` showed the tag
on GitHub at the baked commit, proxy.golang.org served the .info, and a
clean-cache `GOPROXY=direct GOSUMDB=off` install built v4.91.0 stamped
e246971f. Once sumdb answered 200, the default-proxy install verified
and the eleven post-tag re-runs (pipeline/script flags, run keybinding,
serve typed head, codelab runner, from-ssh typed re-entry, console
registration) passed in 66 s. Never re-tag: a 404 from sumdb is a
cache, not a missing tag — check `git ls-remote --tags` first.

## 2026-09-07 (Mon) — three decisions Ross recalled from a killed session: limit dial, typed empties, join swallow

Ross remembered agreeing to these in a session that died before it
journaled; nothing in the transcripts or journals recorded them, so
they were re-stated and built:

**`limit` / `sample` dial.** Bare `limit` (no N) and bare `sample` (no
N, no `-percent`) pass everything through and emit no fragment; `limit
0` is SQL's LIMIT 0 (no records — header-only `to csv`) and `sample 0`
keeps no rows (USING SAMPLE 0 ROWS). Until now `limit 0`/`sample 0`
were the dials (W33), which collided with SQL and `head -n 0`; the
dial is now "delete the number". N carries Default(-1) as the
not-given marker (sample already did). `translateSample` lost its
`n == "0"` skip. Tests flipped: `TestBareLimitSkipsGeneration` (bare
stages vanish from go/sql/ssql in both modes; `limit 0` must appear
as Limit / LIMIT 0 / `ssql limit 0`), equivalence
`limit_bare_passthrough`, `limit_zero_is_empty` (Golden `{}`),
`sample_bare_passthrough`, `sample_zero_is_empty`. Gotcha: DuckDB
1.5.0 prints the malformed `[{]` for an empty `-json` result — the
duckdb lane now reads that as zero rows. DFC110 amended (both the
convention and the workspace's `limit 100000 | sample 1000` window,
whose whole-file dial is now the bare `limit`).

**Typed empties (DFC124 (a)).** Ross: "add an error check to typed
provided it doesn't slow things down too much." An empty cell in a
non-pointer, non-string typed field is now a fatal `ReadError` via
the existing row wrapper — `column "Age": "" is not int64` — from
every typed reader (CSV, parallel, delim; the JSONL positional decoder
shares `decoderFor`, so `"v": ""` into an int field errors too; JSON
`null` is unchanged). Zero cost: the decoders already branched on
`s == ""`, the branch now returns `errEmptyCell` instead of writing 0.
`*T` fields still take nil, `string` keeps `""`. Pointer inference for
partially-empty columns stays future work; remedies documented
(pointer field, `-type COL string`, `fill`). `TestReadCSVEmptyValueZero`
became `TestReadCSVEmptyCellIsLoud`; typed-reference and the DFC124
table say the new rule.

**Join right-side swallow — it was in the assembler, not join.go.**
`replaceForFuncReturn` (lib/codefragment.go) rewrote the first
`if err != nil {…}` of every right-source init fragment to `return
nil`, so ANY template text in join.go was moot: the join got a nil
iter.Seq and died on "invalid memory address or nil pointer
dereference" instead of the open error (so the old behaviour was a
confusing crash, not the silent exit 0 I had written in the TODO —
corrected). The rewrite is now `panic(err)` (main's recover prints
`Error: …`, exit 1) and leaves blocks that already `os.Exit`/`panic`
alone, so join's template — now schema-aware for `.jsonl`, with the
same `Error: opening FILE: …` message as union/merge — survives
intact. `TestJoinMissingRightFileIsLoud` runs the generated program
against a missing right file for csv/tsv/json/jsonl; the union/merge
side-file test gained a join row (count 3, `_schema` header not a
record).

Lesson: when a generated template "doesn't take", read the assembler
— the func-fragment renderer edits fragment code after the command
has emitted it (second instance this week after merge's stmt-fragment
inlining; DFC115's "second implementation" warning applies to the
assembler's own heuristics).

**resample noise (Ross, running section 5 of the codelab: "lots of
resample output … seems a little noisy").** The six-row example
printed three stderr lines: "epoch unit auto-detected as s" and one
clamp note per value field for the single leading grid point. Both
were the loudness doctrine (DFC121) firing on the inherent case:
an epoch-aligned grid starts one point before the first observation,
so `-fill previous` ALWAYS clamps exactly one point, and a 10-digit
epoch being seconds is the obvious reading. Now: the unit note only
for ms/µs/ns; the clamp note only when a field's clamps exceed the
inherent edge count (1, or 2 for linear), as one line naming each
field with its count (`-from 1699999800` on sensor.csv: "8 grid
points … temp (8)"). Tests flipped in resample_test.go (seconds
silent + ms noted; inherent edge silent + `-from` loud). DFC121
amended. Lesson: a warning that fires on every run by construction
is not loud, it is wallpaper — loudness needs a baseline.


**codelab-run.sh ships with the data (Ross: "maybe codelab-run.sh
should be in the codelab data dir").** Moved from `scripts/` to
`doc/codelab-data/` and embedded, so `ssql codelab` writes it beside
the fixtures (mode 0755) and `./codelab-run.sh` becomes an install
self-test: outside the repo it uses the `ssql` on PATH (or
`SSQL_BIN`), takes the doc as an argument or from beside itself, else
fetches `doc/cli-codelab.md` at the tag matching `ssql version` from
raw.githubusercontent.com — so the blocks it runs are the ones written
for that binary. Inside the repo (go.mod two levels up) it still
builds from the checkout, so `make doc-test` / `TestCodelabRuns` are
unchanged apart from the path. Verified all three: in-repo (39/0/10,
signal 34/0/13), shipped from a scratch dir against the dev binary
(fetched v4.91.0's doc, 39/0/10), and TestCodelabRuns. References
moved in doc-test.sh, codelab_test.go, README, doc/README, both CLI
codelabs, the data README, DFC125; CHANGELOG Added.

## 2026-09-07 (Mon) — v4.92.0 released

Ross: "cut a release." Contents since v4.91.0 (all the same day): bare
`limit`/`sample` = pass-through and `limit 0`/`sample 0` = no records;
typed readers reject empty cells in non-nullable fields (DFC124 (a));
generated join reports an unopenable right file (assembler
`replaceForFuncReturn` → panic(err)); resample quiet on ordinary input;
`ssql codelab` ships `codelab-run.sh` as an install self-test; codelab
section 5 says what resample keeps.

Sequence fixed from this morning: ALL gates ran BEFORE the version bump
(race, root/lib/commands, scale, triples, full cmd/ssql suite, explore
e2e, doc-check). The full suite found two stale assertions from the
join change (`TestJoinGeneration*` still expected `ssql.ReadJSON` for a
.jsonl right side; now `lib.ReadJSONLWithSchema`) — fixed in the chore
commit. Then bump + changelog header + explore-wasm refresh → bake
commit.txt → annotated tag → push --follow-tags → debs baked +
committed → install-local both v4.92.0 → WebVM 386 slim pushed to
ssql-terminal + Deploy dispatched. Proxy verify deliberately waited 90 s
after the tag push before the first sum.golang.org lookup (W33/W37
lesson: an early miss is negative-cached for ~15–30 min) — verified on
the FIRST try, stamped 8f9eecad. WebVM deploy green; install-local both
v4.92.0 (gpu: no / yes).

Next (Ross, mid-release): "with distributed data you could have a very
simple catalog file that only refers to data in this directory" — make
codelab §8's skipped `from catalog` block real with local shards.
Scouted while gates ran: `host=local` IS supported (catalog.go
IsLocalHost), but the local shard exec hard-codes `/usr/bin/ssql`
(catalog_remote_go.go:250 and sshRemoteBin) — fails for any go-install
user (`bash: /usr/bin/ssql: No such file`); should use os.Executable()
for local rows. Also: the origin-tag flag is `-shard-field`, not
`-source`; and a field named `date` collides with the expr `date()`
function in `-if-expr` (`date < "…"` won't compile) — use `where -if
date lt …`. Not started; belongs to the next unit, not this release.

## 2026-09-07 (Mon) — codelab §8 runs: a local shard catalog

Ross: "with distributed data you could have a very simple catalog file
that only refers to data in this directory" → "build the local catalog
for section 8". Fixture: `orders.csv` split by month into
`orders_2026-01.csv` (6 rows) / `orders_2026-02.csv` (4) and
`shards.csv` (host,path,format,month; both rows `local`). §8's skipped
block became three real ones: read + group, `-if month ge 2026-02
-shard-field shard` (pruning + origin), `-- where … | count`
(pushdown). Runner: 41 passed, 9 skipped (was 39/10).

The bug it exposed: local rows exec'd `/usr/bin/ssql` — right for ssh
rows (the shell-injection rule wants an absolute path), wrong for a
`go install` user, who has no such file; every local shard died with
exit 127. `ssql.SelfBin(fallback)` now resolves a local row's binary:
the running executable when its basename starts with "ssql" (ssql,
ssql_gpu, test builds), else `ssql` on PATH (a generated program's
executable is the program itself — first attempt used os.Executable()
alone and the generated lane ran `prog` as ssql), else the fallback.
Used by ProcessCatalogShards and startShardSSH (the remote-Go path).
`TestCatalogLocalShardsRunThisBinary` runs exec and a generated record
program against the codelab fixture with PATH restricted (an `ssql`
symlink to the test build, go, /usr/bin, /bin); skips if /usr/bin/ssql
exists since the fallback would mask the bug.

Also recorded (TODO): a field named `date` cannot be used in
`-if-expr` — the expr environment's `date()` function shadows it
(`date < "2026-02-01"` → "mismatched types func(...) and string");
found splitting the fixture, worked around with `where -if date lt`.
And the codelab's reference line said `-source` for catalog; the flag
is `-shard-field`.

## 2026-09-08 (Tue) — v4.93.0 released

Ross: "cut a release." Contents since v4.92.0: `from catalog` local
shards run the installed ssql (`ssql.SelfBin`) instead of
/usr/bin/ssql; codelab §8 runs against the new local shard catalog
fixture (shards.csv + orders.csv split by month); reference line names
`-shard-field`. Gates BEFORE the bump, all green first time: race,
root/lib/commands, scale, triples, full cmd/ssql suite, explore e2e,
doc-check. Then bump + changelog header + explore-wasm → bake
commit.txt → annotated tag → push --follow-tags → debs → install-local
→ WebVM 386 slim to ssql-terminal + Deploy dispatched → playground
workflow green. Proxy verify after the 90 s grace period: first try,
stamped 25e827fc. goreleaser 14 assets; WebVM deploy green; install-local
both v4.93.0 (gpu: no / yes).

## 2026-09-08 (Tue) — remote ssql found wherever it was installed

Ross: "the ssql path seems like a problem we will hit with non-local
ssh — where ssql is only installed on the user we are ssh-ing to and
not generally." Right: every remote command ran `/usr/bin/ssql`
(v4.17's shell-injection hardening chose the .deb's path), so a host
with a go-install (`~/go/bin`) or tarball (`/usr/local/bin`) ssql
failed with bash's "No such file". Recommended and built, all three:

1. `ssql.RemoteBinPrologue(bin)` — the remote command begins with a
   shell prologue that sets `$SSQL` to the first executable among
   /usr/bin, /usr/local/bin, $HOME/go/bin, $HOME/.local/bin (fixed
   ABSOLUTE list: the remote PATH is never consulted, the hardening
   rule holds), or exits 127 with one line naming the host, the paths
   tried and the remedies. `BuildRemoteCommand` and the new
   `RemoteScriptCommand` (ship-and-run) take a bare name → prologue +
   `"$SSQL"`, or an absolute path → used as given. Generated programs
   call the same builders at runtime, so they resolve identically.
2. `-remote-bin PATH` on `from ssh` and `from catalog` (absolute,
   validated by `remoteBinFlag`), threaded through exec and codegen
   (`CatalogShardOpts.RemoteBin`; the ssh landing templates bake the
   path in). `merge -catalog` uses the resolver default.
3. Catalog `bin` column (absolute, per row; round-trips through
   WriteCatalog): a heterogeneous cluster says per host where ssql is.
   `shardBin(entry, remoteBin)`: row bin > local row's SelfBin > flag/
   default.

Verified on the LXD rig for real, not just with the ssh stub: node3
with /usr/bin and /usr/local/bin ssql moved aside and the dev build
pushed to /root/go/bin/ssql — plain read (3), pushdown (2), a mixed
catalog (node3 + local, `-shard-field`), `-remote-bin` explicit, and a
generated record program built against the checkout all pass; with
/root/go/bin/ssql moved too, the message: "ssql: not found on
ssql-node3: tried /usr/bin/ssql /usr/local/bin/ssql /root/go/bin/ssql
/root/.local/bin/ssql; install it there with: go install …, or give
its path with -remote-bin PATH (catalog: a bin column)", exit 127.
Node3 restored (new binary into /usr/bin as rig upkeep, /usr/local/bin
copy back); node1 refreshed to v4.93.0-dev. The stub-ssh tests still
pass because their `sed s|/usr/bin/ssql|BIN|` rewrites the FIRST
candidate in the prologue. `TestRemoteGoIntegration` (SSH-gated) fails
until the next tag — `generate go -run` compiles against the published
module, which has no RemoteScriptCommand yet — same class as
yesterday's pre-tag failures. Unit tests: TestRemoteBinPrologueResolves
(fake ~/go/bin/ssql under a scratch HOME with PATH=/usr/bin:/bin;
nothing installed → 127 + remedies; absolute path → no prologue),
TestReadCatalogBinColumn. CLAUDE.md's remote-path rule now says how:
the builders, never a literal /usr/bin/ssql.

## 2026-09-08 (Tue) — expr: field when bare, function when called

Ross: "what to do about the date problem in expr?" → recommended, he
agreed. Root cause: expr's type checker resolves a bare identifier to a
builtin (`date`, `len`, `type`, `max`…) when the compile environment
does not declare it — and ours cannot, it knows no field names
(AllowUndefinedVariables). So `date > "…"` refused to compile while
`date(date)` worked (the call parses to a BuiltinNode; the argument
resolves at run time). The Go transpiler never had the problem: every
bare identifier is a struct field, only a call position is a function
— so the VM lane was the divergent one.

Fix: `ssql.ExprFieldShadowing()` (expr_shadow.go) — an `expr.Patch`
visitor (same hook as the aggregation patcher) that rewrites a bare
IdentifierNode whose name is in `builtin.Names` (71 of them) to the
MemberNode `$env["name"]`; calls are BuiltinNodes and untouched.
Passed at every compile site: runtime.CompileExpr / CompileExprEnv and
the three expr_agg.go sites. `extractIdentifiers` (first-record field
validation) learned the `$env["x"]` form via `ssql.ExprFieldName`, so a
missing `len` is still "unknown field(s): len". Deleted the dead
`CompileExprWithFields` (an earlier, never-wired attempt at the same
thing). Prototyped first in a scratch module against expr v1.17.6 —
five cases, all right — before touching the tree.

Gates: `TestFieldShadowsBuiltin` (runtime), differential corpus gained
a `date` string field + three expressions (transpiled == VM),
equivalence `expr_field_named_like_builtin` on a new `dated.csv`
fixture — exec, record, typed, parallel, ssql AND DuckDB agree (`date`
as a column name is fine in DuckDB). EXPRESSIONS.md has the rule; the
helper-name exception (`has`, `bucket`, …) is documented, not fixed.

Found on the way (TODO): aggregation expressions are numeric-only —
`-expr 'max(date)'` and a string `-stream-expr` result panic — and the
CLI shows a goroutine dump instead of `Error: …` (generated programs
recover; the CLI main does not).

**String aggregates + CLI recover (Ross: "we need to fix the string
panics").** Two causes behind the two panics. (1) The batch aggregation
env hands `max(date)` the group's date ARRAY (env[field] = []any), and
expr's builtin max is numeric-only. Now the env provides its own
`max`/`min` (`aggMax`/`aggMin` — env functions shadow builtins, the
same trick CompileAggExprPatched already used for `count`), ordering
numbers (as float64), strings and times, flattening arrays or scalars,
refusing mixed types. `CompileAggExprPatched` gets the same dummies so
the typed lowering sees the same tree shape (a CallNode it does not
lower → record fallback, as before). (2) `mustAggFloat64` → `aggResult`:
numbers stay float64 (goldens unchanged), string/bool/time are kept as
typed AggResults; a map/list/nil result still panics, now with an error
VALUE and "need a number, string, bool or time". And the CLI finally
recovers: `cmd/ssql/main.go` prints `Error: …` exit 1 for any panic
(SSQL_DEBUG=1 re-panics for the trace) — the rule generated programs
had since the CellError work. Gotcha in the shadow patcher: with `max`
now an env function, `max(date)` parses as a CallNode whose callee
identifier "max" is a builtin name → the post-order visitor had already
rewritten it to `$env["max"]`; the CallNode visit restores an
identifier callee. Gates: TestExprAggStringResults, the flipped
TestExprAggWrongShapePanics, TestCLIPanicIsAnErrorLine (the test's
first version put `SSQL_DEBUG=1` before the pipeline, which exports it
only to the FIRST stage — `export` it), equivalence
`expr_agg_string_max_min` across the Go lanes (typed falls back to
record and agrees), generated record and typed programs built against
the checkout match exec on orders.csv.


Addendum (Ross: "shouldn't we do first and last while we are here?"):
yes — `first`/`last` join the aggregation env (`aggFirst`/`aggLast`,
arrival order, absent fields contribute nothing as ssql.First/Last do;
the CLI had no -first/-last flags, so -expr is their home). And all
four value aggregates accept an EXPRESSION: the aggPatcher maps a
non-field argument across the group — `max(price * qty)` →
`max(map(_records, .price * .qty))` — a bare field keeps using the
env's field array. Tests: five more rows in TestExprAggStringResults;
the equivalence case grew first/last and an expression max.

## 2026-09-08 (Tue) — v4.94.0 released

Ross: "commit push and cut a release — that is all for today."
Contents since v4.93.0 (one afternoon): remote ssql found wherever it
was installed (`RemoteBinPrologue`, `-remote-bin`, catalog `bin`
column — verified on the rig with a go-install-only node); expr
fields named like builtins usable bare (`ExprFieldShadowing`); string/
bool/time aggregation results, `max`/`min`/`first`/`last` over
strings, times and expressions; CLI panics reported as one `Error:`
line. Gates before the bump, all green first time (race, root, scale,
triples, full suite, explore, doc-check). Sequence as always; proxy
verify after the 90 s grace: first try, stamped 8641b9eb. goreleaser 14
assets; playground + WebVM deploys green; install-local both v4.94.0.
Post-tag, `TestRemoteGoIntegration` (SSH-gated, node1 refreshed to
v4.94.0) STILL failed — and not for the module reason: the generated
program compiled, but the remote shard's program had no pipeline body
("undefined: records"). Cause: `generate go -script` ran the shipped
script's `ssql` stages through PATH, and node1's PATH puts a stale
/usr/local/bin/ssql (v4.28) ahead of /usr/bin — the `# require` check
ran in the new binary, the stages in the old one, whose fragments the
new assembler ignored. runPipelineForFragments now substitutes the
running executable at every stage start (the optimiser's re-execution
already did; same ssqlStageRe). `TestScriptStagesRunThisBinary` runs
-script and -pipeline with PATH stripped of ssql. The rig test passes
with the fix. Committed post-release under Unreleased — a v4.94.x
whenever Ross wants it; v4.94.0's ship-and-run works wherever the
remote PATH is clean, which is every deb/tarball/go-install host that
has ONE ssql.

## 2026-09-08 (Tue) — v4.94.1 released (patch)

Ross: "lets do the patch release." One fix: `generate go -script` /
`-pipeline` run their `ssql` stages with the running binary (the
remote-Go regression found on the rig after v4.94.0). Gates before the
bump, all green; sequence as always; proxy verify after the 90 s
grace: first try, stamped 0df54948. goreleaser 14 assets; playground
green; WebVM deploy dispatched; install-local both v4.94.1.

## 2026-09-08 (Tue) — the codelab from scratch in a mint LXD container

Ross: "spin up a mint lxd container and do the codelab from scratch to
see what problems you hit." `lxc launch ubuntu:24.04 ssql-codelab`, then
Setup verbatim as the `ubuntu` user (sudo, no Go, tmux 3.4 already in
the cloud image). Timings and findings:

- `apt-get install golang-go` 40 s → Go 1.22.2. `go install …@latest`
  53 s: "requires go >= 1.26; switching to go1.26.8", 926 MB of module
  cache (240 MB of it the toolchain). `ssql version` → v4.94.1. The
  PATH line and `ssql codelab` (14 files) behaved as written.
- Interactive keys, driven through a real pty in a bash with the eval
  line (no tmux, inline mode): Tab completes the command; Tab in a
  field slot inserts `Use-Ctrl-O` exactly as §1 now says; Ctrl-O lists
  the field names and the `dept` values; Alt-h prints `-sum`'s help.
- **`./codelab-run.sh`: 40 passed, 1 FAILED** — the §7 `generate go
  -run` block: "go: download go1.23 for linux/amd64: toolchain not
  available". Root cause: compileGoSource wrote `go 1.23` (a bare
  minor) into the temp module; Go 1.22 tries to fetch a toolchain named
  exactly go1.23, which does not exist (only go1.23.0…). Verified in
  the container: `go 1.23` and `go 1.26` both fail that way, `go 1.26.0`
  downloads 11 s, `go 1.26.8` (already cached from the install) is
  free. Fix: `generatedGoLine()` = the full version of the toolchain
  ssql was built with (runtime.Version()), so a go-install user's temp
  module asks for the toolchain they already have; devel builds fall
  back to 1.26.0. With the dev binary pushed in, the block passes: 35 s
  the first time (toolchain + deps compile), 1.5 s the second. Runner
  now 41/0/9 on the mint machine. Every go-install reader on Ubuntu
  24.04 would have hit this on their first §7 command — the review
  would have found it, but better us.
- `generate sql -run` → "exec: duckdb: executable file not found" —
  fine as an error; §7 now says the SQL block needs DuckDB.
- Docs: Setup's prebuilt-binary paragraph says §7 needs `go`; §7 says
  what its first run downloads and how long it takes.
- Curiosity, benign: `go install` also fetched `github.com/rosscartlidge/
  ssql v1.22.0` — module-path resolution probing the v1 prefix while
  locating `…/v4/cmd/ssql`; not in our graph.

Not exercised: the tmux popup rendering itself (needs a real terminal),
`ssql serve`, `from ssh`. Container `ssql-codelab` left RUNNING for
Ross to poke at (`lxc exec ssql-codelab -- su - ubuntu`); it has the
dev binary in ~/go/bin/ssql, not the released one.

## 2026-09-08 (Tue) — v4.94.2 released (patch)

Ross: "lets cut 4.94.2." One fix from the mint-container run: the
`generate go -run`/`-build` temp module names the full version of the
toolchain ssql was built with, so a stock Go 1.22 no longer asks for a
non-existent `go1.23` toolchain; plus the codelab §7 prerequisite
sentences. Gates before the bump, all green; sequence as always; proxy
verify after the 90 s grace: first try, stamped d09c2a8c. goreleaser 14
assets; playground green; WebVM deploy dispatched; install-local both v4.94.2.

Addendum — Ross: "when it's pushed, remove the ssql-codelab lxd, make a
fresh one, and test the new codelab" + "write a doc on what we did".
Both: `scripts/codelab-mint.sh [-k] [NAME]` (+ `codelab-mint-keys.py`,
`make codelab-mint`) scripts the whole exercise — launch ubuntu:24.04,
Setup verbatim as `ubuntu`, pty-driven keys, the shipped runner, the
skipped `generate go -run` block, timings, PASS/FAIL — against the
PUBLISHED release, nothing from the checkout. Fresh container on
v4.94.2: PASS — 41/0/9, all five keys, `generate go -run` 2 s / 1 s
(the runner had warmed it). DFC126 records method, both runs, the
`go 1.23` finding and the lessons (a skipped block is an untested block;
the author's toolchain hides version bugs; test the published artifact).
Container left running. Uncommitted.

## 2026-09-10 (Wed) — first codelab review comment: Tab twice

Reviewer via Ross: "why do you need to hit <tab> twice — `ssql <tab>`
does nothing, any <tab> after that gives the commands." Readline, not
ssql: with every command matching and no common prefix, bash rings the
bell on the first Tab and lists on the second (`show-all-if-ambiguous`
off by default); `ssql fr<Tab>` completes at once. Confirmed through a
pty in the mint container (one Tab → `\x07`; two → the list; one with
the setting on → the list). Our shell-init sets no readline options and
the tmux popups are Alt-h/g/r only, so Tab is plain readline everywhere.
Fix chosen: the doc — `ssql <TAB><TAB>`, a paragraph on why, and the
`bind 'set show-all-if-ambiguous on'` line as an OPTIONAL ~/.bashrc
addition. Rejected: emitting that bind from -shell-init — it changes
completion for every program in the user's shell.

Second review comment (same day): "more guidance on what formats are
supported, and be told early that the default output is JSONL and what
it looks like." §2 now has a "What flows between the stages" block —
`ssql from employees.csv | ssql limit 2` and its two-line output, the
`_schema` header explained, "a `to` stage is the only format change",
`> saved.jsonl` / `ssql from saved.jsonl` / `| jq` — and a "Formats"
paragraph: readers by extension (csv tsv json jsonl parquet arrow xlsx
wav; .log/.txt as lines), `from csv FILE` / `from jsonl -` when the
extension lies or the input is a pipe (both verified), the sinks
including `markdown` (which §9's list had missed) and the visual three.
Runner 42/0/10.

Third review comment: "they didn't know why `ssql version` / `ls` had
to run." The block said only "Check what you have". Now: tmux gave you
a new shell — confirm it finds ssql (the ~/.bashrc PATH line at work;
what to type if it does not) and that you are in the data directory.
Pattern across all three comments so far: every step needs its *why*
in the sentence before it; a bare command reads as a ritual.

Ross: say `generate sql` is the DuckDB dialect. §7 now does, and names
what in the generator is DuckDB-specific (grep of generate_sql.go:
`FROM 'file'`/`read_csv`, `USING SAMPLE`, `UNPIVOT`, `EXCLUDE`; the rest
— GROUP BY, ORDER BY, LIMIT, joins, `regexp_matches`, ROLLUP — is
PostgreSQL-flavoured SQL).

Ross: do we say ssql should be installed on the machines you ssh-fetch
from? Only the skip marker did. It is a requirement, not a nicety —
`from ssh` runs ssql on the host — so §8 says that before the block,
with the why (the filter runs where the data is), where the binary is
looked for, `-remote-bin`, and the ssh-config reachability; the catalog
paragraph now refers back instead of repeating the list.

## 2026-09-10 (Wed) — `update -set-bucket`: bucket gets a flag spelling, and SQL

Reviewer asked about the expr `bucket` function; Ross: "as bucket is
very important do you think it should be part of update — like `ssql
update -set-bucket minute ts 1m`?" Yes, as a spelling: new field first
like -set/-set-expr; desugars to `bucket(ts, "1m")` in ONE place
(`clauseSetExprs`, now shared by update's exec, record-codegen and
typed paths — the three duplicated -set-expr walks collapsed into it),
so every lane that handled the expression handles the flag; the
`where -if` / `-if-expr` pairing pattern. Width validated at parse time
(positive Go duration) in every mode. Sites taught the new flag: schema
op (new field), the SQL translator (`-set-bucket` → same exprToSQL),
the optimiser's read-field extractor (reads SOURCE), the completion
config test. DurationCompleter on WIDTH.

Two things found on the way, both fixed: (1) `bucket()` had no SQL
translation — now a CASE mirroring `DetectEpochUnitNanos` (abs(ts) ≥
1e17 ns / 1e14 µs / 1e11 ms / else s) snapping with `%`, width rendered
as an integer in that unit when it divides evenly so integer columns
stay integer; strings refused as for resample. (2) `generate sql` with a
BARE `from FILE.csv` never seeded column tracking (only `from csv FILE`
did), so any `update -set-expr NEW …` emitted `* REPLACE ("NEW")` —
DuckDB "Binder Error: Column not found" — pre-existing, hidden because
every equivalence case used the explicit form. Now seeded by extension.

Gates: three new equivalence cases on new `epochs.csv` / `epochs_ms.csv`
fixtures (flag vs expr forms with goldens, ms unit detection) agree in
all six lanes including DuckDB — my first goldens were wrong (mis-binned
…044 and …059), the lanes were right; schema-op test row; codelab §5
uses the flag; EXPRESSIONS.md date table gains bucket + the flag;
DFC121 amended.


## 2026-09-10 (Wed) — v4.95.0 released

Ross: "cut a release." Contents since v4.94.2: `update -set-bucket FIELD
SOURCE WIDTH` (flag spelling of bucket()), bucket()'s SQL translation,
the bare-`from` column-seeding fix in the SQL translator, the codelab
review edits (Tab twice, JSONL shown early + formats, the two checks
explained, DuckDB dialect, ssql on ssh hosts, §5 on the flag), and
`scripts/codelab-mint.sh` (DFC126). Gates before the bump, all green;
sequence as always; proxy verify after the 90 s grace: first try,
stamped 9e7df775. goreleaser 14 assets; playground + WebVM deploys
green; install-local both v4.95.0.

Review comment: "didn't understand the use of clauses to do ORs and
ANDs." The codelab had one dense sentence and one OR example. Now a
short section in §2 — AND (all `-if` in a clause), OR (`+` starts a new
clause; read it as "or, alternatively"), NOT (`+if`) — each a runnable
block with the expected row count in the comment (3, 6, 1 on
employees.csv), the one-line rule, and that `update -if … -set …` uses
the same grammar. Runner 44/0/10.

## 2026-09-10 (Wed) — `where -not` / `-invert`, and two old bugs they exposed

Ross, from the clause discussion: a clause-local `-not` and a global
negation. Agreed, with one rename: his `-not-all` reads in English as
"at least one fails" while the semantics are "every clause fails", so
`-invert` (grep's -v). `-v` itself is impossible — the root command's
`-verbose` alias, autocli refuses the conflict — so `-invert` has no
short form for now. Built: exec, record + typed codegen (shared
`joinClauseConds` / `whereBody` helpers, replacing two copies of the
AND/OR assembly), SQL (`NOT (…)` per clause / around the whole), and
`update -not` on its clauses (exec, record, typed, SQL CASE arms).
`-not` with nothing to negate is a parse-time error in every mode;
`-invert` on an empty where matches nothing (exec had an early
`return true` before the inversion — caught by hand, fixed). The
optimiser treats any negated `where` as opaque (`whereHasNegationFlags`
guards every rule that parses/rebuilds clauses; column-pruning answers
"unknown"); pushdown moves the stage verbatim so still applies. Six
new equivalence pairs (flag vs `-if-expr` form) agree in all six lanes.

Two pre-existing bugs fell out of the same fixture: (1) `ruleWhereMerge`
concatenated argument lists, so `where A + B | where C` became
A OR (B AND C) — 5 rows instead of 4 on employees.csv, in EVERY
generated-Go lane, for as long as the rule has existed; now merges only
when both stages are single-clause. (2) With the merge gone, the SQL
lane showed the same wrong answer: translateWhere appended a
multi-clause OR to `whereClauses` unparenthesised, and they are ANDed
at render time. New equivalence case `where_or_then_where_is_and`
pins both. Lesson: the equivalence gate had no case with TWO where
stages where the first had two clauses — every case was one stage, or
single-clause — so "all lanes agree" was true and wrong together; a
shape that is not in the corpus is not tested.


## 2026-09-10 (Wed) — v4.96.0 released

Ross: "commit and push and cut a release." Contents since v4.95.0:
`where -not` / `-invert`, `update -not`, the where-merge and SQL
multi-clause fixes, codelab §2 clause grammar (AND/OR/NOT + the two
flags). Gates before the bump, all green; sequence as always; proxy
verify after the 90 s grace: first try, stamped 0b67d221. goreleaser 14
assets; playground + WebVM deploys green; install-local both v4.96.0.

## 2026-09-10 (Wed) — Alt-h: precise help for the function under the cursor

Ross: when does Alt-h show expr syntax, and "we need a way of getting
both the general function help and the precise help for a function".
Mechanics: `_ssql_help_at` cuts the line at the cursor, works out the
COMP-style word index and calls `-help-at POS words…`; on an
`Arg().Expression()` argument (ExprArgAtCursor, DFC116 F3) the concise
FunctionsReference was appended — always the whole thing. Now the word
the cursor is on arrives cut at the cursor, so `exprFunctionCandidates`
reads the identifier being typed and the innermost unclosed call around
it (`upper(trim(name` → [name trim]; name is a field, so trim's entry
shows); the first with a `FunctionEntry` wins and replaces the reference
with that one block plus a pointer to the full one; no match → full
reference as before. To lift one entry out, the ten category printers
became writers on io.Writer (`writeDateFunctions(w)` …), `functionCatalog`
renders them into one string and `FunctionEntry(name)` cuts the block
that starts `  name(`. `ssql functions NAME` prints the same entry.
Found on the way: the detailed reference had no `bucket()` at all —
added, and a test now asserts every function in the concise list has a
detailed entry. Gotcha: -help-at's pos counts the program name at 0,
so the word is rest[pos-1] (first attempt indexed rest[pos] and fell
through to the full reference every time).

Review comment: "Open /tmp/salary_by_dept.html" failed for a reviewer
with snap Chrome. Snap-confined browsers get a private /tmp, so a file
another program wrote there is invisible to them — launcher-independent;
xdg-open usually gets round it via the portal, but the robust fix is to
not write into /tmp: the codelab's chart now lands beside the data, and
the text gives `xdg-open` (xdg-utils, on every desktop Linux) / macOS
`open` / File → Open, plus the one-line reason. tee's /tmp checkpoint is
fine (nothing opens it in a browser).

## 2026-09-10 (Wed) — signal codelab: the reader's pass

Ross: "have you worked through the signal processing codelab to make
sure it works?" Honest answer: the runner had (34 blocks on every
change, in the release gates), a reader had not. Read all 1025 lines
with the review lens and rewrote what it showed:

- It opened "GPU-Accelerated Tutorial … 10-50x faster" and put two
  clone-and-build CUDA recipes before the first command. Now: CPU
  first, GPU last — a Performance Tips section absorbs both recipes
  and says WHY a clone is needed there (CUDA is built from source;
  every other route is CPU-only) and when to stop reading (signals
  under ~16K samples never touch the GPU anyway).
- No Setup: python3 was mentioned in passing though 12 blocks depend
  on it. Now a Setup section — ssql per the CLI codelab, python3 (why,
  how to check, apt/brew), an empty directory as a step, how to open
  the charts (beside the data, xdg-open/open; the snap /tmp note), the
  CLI codelab assumed, `ssql version` + `python3 --version` as the
  check.
- The spectrogram "Output Formats" block ran two spectrograms with no
  sink — thousands of JSONL rows to the terminal; `| limit 3 | to
  table` now. The autocorrelation tip called the full computation
  "O(n²) via FFT" — reworded. Example 4 says ffmpeg is the only extra
  tool and the runner skips those blocks.
- Kept: charts already land beside the data (no /tmp trap), every
  block has a comment saying what it does. Found, not fixed: `convolve
  -custom "-1,-1,…"` is a comma list in one flag (rule violation; TODO).

Runner: 35 passed, 13 skipped. The shipped runner takes `cli`/`signal`
as names; codelab-mint runs both, and gained `-b BINARY` so the fresh
container can exercise a pre-release build — used now, since v4.96.0's
embedded runner does not know `signal`. README learning-path entry and
doc index reworded (no "GPU-accelerated" as the headline).

