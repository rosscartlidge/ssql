# Journal — 2026-W32

## 2026-08-09 (Sun) — ported generate-ssql negation fix from the web-session branch

While away, a claude.ai/code web session independently fixed the v4.56
`+if`/`+if-expr` negation class on branch
`claude/journal-files-context-ynn0os` (25a61cf) — the same sweep later done
locally as 2d890de and shipped in v4.56.1. The two are rival rewrites of the
same files (different shared-helper names: web `parseExprConditions` vs
main's shipped `parseExprConds`), so merging the branch was wrong. Instead:
keep main canonical, port the branch's one UNIQUE piece.

**What was uniquely on the branch:** `cmd/ssql/commands/generate_ssql.go` —
the optimiser's where round-trip didn't recognise `+if`/`+if-expr`
(`parseWhereArgs` fell through to default and skipped the tokens), so ANY
rewrite rule that rebuilt a where clause (predicate simplification, reorder,
catalog extraction, join pushdown) silently dropped the negated conditions
from the optimised pipeline. Confirmed live on main: v4.56.1's equivalence
cases all use single-condition wheres, which never trigger a rebuild — the
ssql-opt lane passed while the bug sat one rewrite away. Main hadn't touched
the file since the branch point (16bc3b7), so the port was a clean
`git checkout 25a61cf -- cmd/ssql/commands/generate_ssql.go`.

**Gates, watched failing first:** two new equivalence cases —
`where_negated_survives_simplify` (`-if pop gt 5 -if pop ge 8 +if pop lt 12`:
range tightening triggers the rebuild; also proves the negated bound stays
OPAQUE to tightening) and `where_negated_expr_survives_reorder` (`ne` is
cheaper than `gt`, reorder triggers the rebuild). Both failed exactly and
only in the ssql-opt lane pre-port, green in all lanes post-port. Also
ported `TestNegatedConditionGeneration` (branch's emitted-source pinning
test — main's emissions matched its expected strings verbatim) extended
with two `generate ssql` round-trip subtests, verified discriminating
(optimiser output shows the rule fired AND the `+` token survived).
Full equivalence + permutations + corpus + commands tests + vet green.

**Found on the way (TODO'd, not fixed):** record codegen mis-references
flags for duplicate field+op conditions — `-if pop gt 5 -if pop gt 8`
declares `flagPopGt`/`flagPopGt2` but emits both references as
`flagPopGt2` (first value silently replaced; three duplicates → undeclared
`flagPopGt32`, doesn't compile). Invisible for ANDed same-direction bounds,
wrong for `+if` mixes and OR clauses. The first draft of the simplify
equivalence case tripped over it; reshaped the case to distinct operators
so it gates the optimiser bug, and recorded the naming bug in
doc/research/TODO.md. Also ported the branch's TODO about `-catalog` `-if`
pruning flags having no negated form.

The web branch (`origin/claude/journal-files-context-ynn0os`) is now fully
superseded: its where/update/helpers changes shipped in v4.56.1 via 2d890de,
its generate_ssql.go + tests + TODO are ported here. Safe to delete after
this lands.

## 2026-08-09 (Sun) — fixed the duplicate field+op flag-naming bug

Followed up the TODO filed this morning: record codegen for `where` with
duplicate field+op conditions silently replaced the first value with the
last (`-if pop gt 5 -if pop gt 8` → both references emitted as
`flagPopGt2`), and three duplicates didn't compile (`flagPopGt32`,
undeclared). Root cause was TWO stacked defects:

1. `generateCondition` (where.go) derived flag var names from field+op
   only, so duplicates collided within a fragment.
2. `collectParams` (lib/codefragment.go) uniquified declarations but
   rewrote references with a bare `strings.ReplaceAll` — which (a) cannot
   distinguish two identical `*flagPopGt` references in one fragment (both
   got the last name), (b) corrupted by prefix (`*flagPopGt` matched inside
   `*flagPopGt2` → `*flagPopGt32`), and (c) never registered renamed names,
   so a rename could collide with a genuine later name.

Fix mirrors the layering: emission-time numbering of duplicate field+op
names in `generateCondition` (within-fragment uniqueness is the emitters'
contract — identical references are indistinguishable to any later textual
rename), plus a two-pass `collectParams` (claim all keepers first, THEN
rename collisions to the first free suffix, rewriting with
`\*name\b` word-boundary regex). The two-pass shape matters: my first
attempt renamed in one pass and the new `TestCollectParamsRename` unit test
immediately caught a rename landing on a name a later param in the same
fragment still held — recreating the double reference one level up.

Gates, each watched failing against the exact defect it guards:
`where_dup_fieldop_negated` (silent wrong results, 2 dups + `+if`),
`where_dup_fieldop_three` (compile failure), `where_dup_fieldop_two_stages`
(passes pre-fix — it exists to fail against the emission fix WITHOUT the
collectParams registry fix, verified by temporarily restoring the old
collectParams). Typed codegen inlines condition values and was immune;
exec / generate sql / generate ssql don't use Go flags. Full equivalence +
permutations + corpus + lib/commands tests + vet green.

## 2026-08-09 (Sun) — expr transpiler Phase 1 shipped

Started the expr→Go transpiler (the plan in
doc/research/expr-transpiler-implementation-plan.md, written 2026-07-03).
Phase 1 complete in one session: `exprToGo`/`exprToGoBool`
(cmd/ssql/commands/expr_go.go, mirroring the proven exprToSQL walker) +
tiny `exprfn` runtime package (Abs, RuneLen — Go's builtin min/max cover
the rest), wired into typed `where -if-expr` and typed `update
-if-expr`/`-set-expr`. Both sites now try native transpile first and fall
back to record codegen per expression (handled=false contract) instead of
the old whole-stage downgrade (where) / hard Tier-3 error (update).
Unknown fields stay LOUD (`exprUnknownFieldError`) — a typo fails at
codegen listing the schema, not at row time.

Findings worth remembering:
- **The parser desugars chained comparisons** (`1 < x < 3` arrives as `&&`
  of two BinaryNodes) and pipe syntax (`x | upper()` arrives as the
  builtin) — two planned "open questions" that needed zero code.
- **The differential harness earned its keep on its FIRST run**: the plan's
  §2d table said mixed int/float `min()` promotes to float64; the VM
  actually returns the WINNING operand with its own type (min(-3, -2.5) is
  int64 -3). No static Go type expresses that → mixed min/max refuses and
  falls back. Plan table corrected.
- **`-set-expr` into an existing int64 column with a float result is a
  RETYPE, not a coercion** — exec makes pop/2 into 3.5; emitting int64(…)
  truncation would silently diverge. Falls back to record mode. Only the
  value-preserving int64→float64 widening is inserted.
- Watched-failing gates: sabotaged the division emission to Go integer
  division — the harness flagged `pop / qty` native=3 vm=3.5 exactly as
  §7.6 demands.

Numbers (TestExprGoNativeZeroAllocs, 1M rows): predicate 4ns/op native vs
1.50µs VM (~375x), assignment 3ns vs 1.31µs (~435x), 0 allocs/op both.

Acceptance per plan §8: update_set_expr + update_if_expr_only equivalence
cases run go-typed/go-parallel UNSKIPPED; added update_set_expr_division
(the float-division golden through every lane incl. DuckDB) and
update_set_expr_ternary; whereexpr stage added to the permutation gate
(all 10 pairs green — note the stage may only reference `pop`, the one
field group-by retains). Full equivalence + permutations + corpus +
commands/lib tests + vet green. CLAUDE.md got the transpiler rule
one-liner.

Next: Phase 1.5 — Tier V (runtime.CompileExprEnv + generated static env)
so untranspilable expressions keep the pipeline typed, and `-explain` tier
reporting.

## 2026-08-10 (Mon) — expr transpiler Phase 1.5: Tier V + -explain

Tier V shipped: an expression outside exprToGo's native subset no longer
ejects the stage (and everything downstream) to record mode. The generated
typed code evaluates it with the expr-lang VM against a static env built
from the struct — `runtime.CompileExprEnv`/`CompileExprFilterEnv` in the
new `runtime/env.go` (CompileExpr minus the Record→env copy; has/getOr
close over the FIELD map so helper names aren't visible as fields), plus a
generated per-schema env constructor (`exprEnvWRow(r)`) and hoisted
compiled-VM vars, all content-addressed and assembler-deduped. Verified
end-to-end: `where -if-expr 'sha256(city) > "8"' | group-by` in parallel
mode keeps GroupByParallel — the whole point.

Design decisions:
- **-set-expr Tier V is existing-columns-only**, typed at runtime by loud
  `runtime.MustCoerce*` helpers (exact match or int64→float64 widening;
  anything else = a retype → stderr + exit 1 pointing at SSQL_MODE=record).
  A NEW field from an untranspilable expression has no knowable Go type →
  record fallback, as the plan's §10 predicted.
- **Codegen validates VM-compilability before emitting Tier V** — an
  expression invalid in every mode errors at codegen listing the parse
  error, instead of panicking at generated-program startup.
- **-explain tier lines ride a new CodeFragment.PlanNotes field** (fragments
  cross process boundaries — the where command can't see generate go's
  flag), printed centrally in AssembleCodeFragments under
  SSQL_EXPLAIN_PLAN: `expr "pop > 8": native`, `expr "sha256(city)": VM
  with static env (function "sha256" has no native Go emission)`, `record
  fallback (reason)`.

Gotchas:
- Parallel programs import stdlib `runtime` (GOMAXPROCS) — collides with
  `cmd/ssql/lib/runtime`. Both assemblers' import renderers now accept
  "alias path" entries; Tier V imports as `exprvm`. (First attempt also
  double-quoted the alias path — invalid import — fixed.)
- `generate go -run` pins the PUBLISHED module version in its temp go.mod,
  so new runtime APIs (exprfn, env.go) fail under -run until the next
  release. Pre-existing behaviour (any new API), test harnesses use
  replace directives. Worth remembering when smoke-testing pre-release.

Gates: TestTierVKeepsTypedPipeline (parallel group-by survives; record
markers asserted ABSENT; -explain assertions incl. the fallback note),
equivalence where_expr_tier_v (sha256 golden — 5/12 cities precomputed,
duckdb lane skipped: sha256 has no SQL translation by design) +
update_set_expr_tier_v, runtime env/coerce unit tests. Full equivalence +
permutations + corpus + vet + doc-check green.

Next per plan §8: Phase 2 — group-by -stream-expr as a typed accumulator.

## 2026-08-10 (Mon) — expr transpiler Phase 2: -stream-expr typed accumulators

`group-by -stream-expr INIT EVERY FINAL RESULT` now lowers onto the same
synthesized aggregator struct the built-in aggregations use
(`lowerStreamAgg` in expr_stream.go): init keys → typed state fields with
init values in the aggregator CONSTRUCTOR (`&T{}` zero value is wrong for
`{s:1}`), every → Add() statements, final → the Result() expression
(float64-coerced, mustAggFloat64 parity). Mixes freely with -count/-sum/…
in one aggregator. Forces the serial group-by form (SerialOnly fragment —
fold state is not mergeable; the planner serialises upstream and -explain
says why).

The two semantics traps, both caught by reading evalStreamAggExpr before
writing code:

1. **Record SHADOWS state** — the env build is maps.Copy(state) then
   maps.Insert(record), so a state key colliding with a schema field
   resolves to the RECORD in every-exprs. The plan's §5c assumed the
   opposite and (wisely) said "VERIFY at impl". Corrected in the plan;
   locked by the record_shadows_state unit case.
2. **Every's object replaces the state SIMULTANEOUSLY** — `{a: b, b: a}`
   must swap. Emitted as ONE Go multi-assignment; the
   groupby_stream_swap equivalence golden (flip=0 after 12 rows vs 1 for
   the sequential bug) was watched failing against sabotaged sequential
   assignments — both typed lanes diverged, exactly as designed.

Also: type inference runs to a widening fixpoint (init `{s:0}` + every
`{s: s+pop/2}` widens s to float64 — division semantics inside folds gated
by the groupby_stream_widening golden, 199/2=99.5). Every-keys ≠ init-keys
is a refusal (the VM legitimately reshapes its state map; a struct can't)
→ record fallback with -explain note. duckdb lane skipped on all four new
equivalence cases: generate sql rejects -stream-expr loudly (v4.56.0).

Gates: TestLowerStreamAgg, TestStreamExprTypedGeneration, equivalence
groupby_stream_{avg,widening,swap,grouped} + full equivalence +
permutations + corpus + vet + doc-check green.

Next per plan §8: Phase 3 — group-by -expr via the aggregation patcher's
normal form (mergeable accumulators, KEEPS GroupByParallel).

## 2026-08-10 (Mon) — expr transpiler Phase 3: -expr mergeable accumulators

`group-by -expr` aggregations now lower to native typed accumulators that
KEEP GroupByParallel — the reason Phase 3 exists. `lowerExprAgg`
(expr_agg_lower.go) walks the patcher normal form: each distinct
sum(_records, elem) → a `+=` accumulator field (deduped by transpiled
element source), count() → a shared counter, the outer arithmetic →
Result() over placeholder identifiers substituted into the tree. Merge
adds terms and counts (associative) → dual templates preserved; watched
the gate fail by sabotaging Merge — the go-parallel lane diverged on the
avg golden.

Three lessons from the session:

1. **Don't re-parse what exec compiles — reuse exec's compile.** Bare
   `count()` is a PARSE error under parser.Parse (the arity-checked
   builtin); it only parses in exec because buildAggBatchEnv installs a
   dummy `count` env function that shadows the builtin. First attempt
   re-parsed + re-patched and every `count()` silently fell back to record
   (caught because the smoke test greped for an accumulator that wasn't
   there). Fix: export `ssql.CompileAggExprPatched` — exec's own
   expr.Compile with the same env dummies and patcher — and lower from
   `program.Node()`. One compile path, two consumers.
2. **Accumulators keep the element's own type.** The plan's §5d said
   `accN float64`; but the VM's sum of ints is an INT, and `sum(pop) % 5`
   must stay integer modulo (a float accumulator would refuse `%` and
   silently fall back). Locked by the groupby_expr_int_mod golden
   (199 % 5 = 4).
3. **A field OUTSIDE an aggregation is legal exec** — the batch env binds
   it to the group's value ARRAY, so `sum(salary)/len(salary)` works (len
   of array = group size). No typed lowering exists → REFUSAL (record
   fallback preserves the behaviour), deliberately NOT the loud
   unknown-field error that a typo inside sum() gets. Getting loud-vs-
   fallback wrong here would have broken working pipelines in typed mode.

Gates: TestLowerExprAgg, TestExprAggTypedGeneration (emission + parallel
kept + fallback note), equivalence groupby_expr_{avg,arith,int_mod,
grouped} with goldens, Merge sabotage watched failing. Full equivalence +
permutations + corpus + root-package tests + vet + doc-check green.

Phases 1–3 of the transpiler are now done. Remaining: Phase 4
(record-mode native where/update) and Phase 5 (breadth: time.Time,
string→int, split/join/replace via exprfn — grow from -explain
telemetry). Phase 1.5's Tier V means Phase 4 is now purely a performance
win, not a correctness one.

## 2026-08-10 (Mon) — expr transpiler Phase 4: record-mode native

Record pipelines get native expressions too. The plan's §5e sketched
"whole-expression inference" over unknown field types; what actually
shipped is better: REAL types. `from csv` now samples column types in
record mode (same lib.SampleCSVSchema typed mode trusts) and carries them
as a new `AdvisoryTypes` map on the fragment — deliberately NOT
OutputTypedSchema, which would flip the pipeline into the typed
assembler. The walker grew a record resolver emitting typed GetOr calls;
with advisory types, `where -if-expr` emits a native predicate and
`update -set-expr` collapses eval + the per-row runtime type-switch into
ONE typed setter (result type known at codegen; native expressions are
total, so no eval-error path).

Design calls worth remembering:
- **Exact per-type GetOr emission, not a float64 blanket** — int
  arithmetic and % keep VM parity. The GetOr conversion semantics
  (float→int truncates, string parses, silent default) make the emission
  type LOAD-BEARING; the well-typed-column contract matches typed mode's
  and the existing -if GetOr emission's.
- **Advisory propagation is conservative**: where passes it through;
  update tracks assignments and DROPS a field on retype
  (`-set-expr salary 'salary/2'` on an int column → no downstream
  expression may use the stale int64 type — gated by a generation case);
  new fields aren't added (conditional clauses can leave them absent);
  every other command simply doesn't propagate → downstream uses the VM,
  zero regression, reason under -explain.
- **Unknown fields are a refusal in record mode, not loud** — rows may
  have been reshaped mid-pipeline and the VM validates against the real
  first record. (Typed mode stays loud; it has schema truth.)
- **matches (hoisted regexp) stays on the VM in record mode** — the
  record assembler has no package-level decl slot. TODO if telemetry
  shows demand.

Fallout: TestNegatedConditionGeneration's record pins asserted the VM
filter vars; record -if-expr now emits native negation
(`return !((ssql.GetOr(…) > 25))`) — pins updated, semantics unchanged.

Numbers: record-native predicate 47ns/op, 0 allocs vs 1.52µs VM (~32x);
typed native remains 4ns (GetOr pays its schema lookups).

Gates: TestExprToGoRecord, TestRecordNativeExprGeneration (native
where/update, VM-without-advisory + note, propagation, retype safety),
full equivalence (record lanes now natively generated end-to-end) +
permutations + corpus + vet + doc-check green.

That completes plan phases 1–4 in two days against an estimated 5-8.
Remaining: Phase 5 (breadth — time.Time lowering, string→int casts,
split/join/replace via exprfn, record `matches`, JSONL advisory types,
wider advisory propagation), to be driven by -explain fallback telemetry
rather than speculation.

## 2026-08-10 (Mon) — benchmark campaign + experience-report paper

Ran a real measurement campaign on the transpiler (5M-row / 115 MB CSV,
Ultra 9 275HX 24T) comparing interpreted exec, generated code from the
ACTUAL pre-transpiler commit (b1adab9, built via a temp worktree), and
current main. Wrote it up as doc/research/expr-transpiler-paper.md
(indexed in doc/research/README.md).

Headline medians (3 runs, /usr/bin/time -v, outputs verified equal
before timing):
- Micro: predicate 3.0ns/0B typed, 28ns/0B record-native, vs 1,261ns +
  1,088B/9 allocs VM (Tier V ~same as VM: 1,245ns/12 allocs).
- E1 filter+count: pre-gen 8.4s/1.18GB (and "parallel" mode pre-Phase-1
  ran at the SAME speed — the cliff, measured) → parallel-native
  0.44s/329MB = 19× CPU, 3.6× memory. Record-native 3.16s = 2.7×.
- E2 -expr agg: pre 6.9s/2.45GB (typed mode: hard error) → 0.43s/328MB
  = 16× CPU, 7.5× memory + capability unlock.
- E3 -stream-expr fold: pre 6.95s/1.80GB → typed serial 1.68s/29MB =
  4.1× CPU, 62× MEMORY (streaming accumulator vs materialized groups).
- Honest footnote: E2 parallel agrees with serial only to ~12
  significant digits (float summation order across shards).

Paper's judgement: worth it decisively — the CLIFF removal (one
expression no longer downgrades the whole pipeline) and the memory
capability boundary matter more than the constant factors; and the
technique (curated-subset transpilation + tier ladder + differential
oracle + sabotage-tested gates) generalizes to any system embedding a
dynamic expression language in generated code. Costs stated: ~2.3k impl
/ ~1.5k test LOC, 2 days, 1.4s compile amortization, well-typed-column
contract in record mode.

Benchmark hygiene: pre-binary worktree removed after the campaign; all
bench programs/data live in the session scratchpad only.

## 2026-08-10 (Mon) — flag-vs-expression convergence proposal

Discussion after the paper: does the transpiler's success obsolete the
flag forms (-if/-set)? Answer (agreed): no — their value shifted from
performance (now matched) to (1) the interactive surface (field/op/VALUE
completion, zero shell quoting, SSH/pushdown robustness), (2) being the
optimizer's IR (every generate-ssql rewrite and total SQL translation
operate on {Field,Op,Value} and bail on expressions), (3) predictability
(no tier ladder, plus record codegen's runtime-parameterized flags).

What SHOULD change: -if is lowered independently in five places (exec
applyOperator, record generateCondition, update generateConditionCode,
typed typedWhereCondition — no regex — and SQL translateWhere) — the
exact duplication class behind the six-site +if negation bug. Wrote
doc/research/flag-expr-convergence.md: keep both surfaces, converge the
lowerings onto the transpiler (condToExprGo + a parameter-reference leaf
to preserve CodeParam flags), Phase A FIRST = metamorphic flag≡expr
equivalence gates per operator (standalone value; likely to expose
latent divergences), exec's applyOperator deliberately left as the
untouched differential oracle. Unlocks regex-in-typed and optional
normalization of trivial -if-expr into optimizer-visible conditions.
TODO'd + indexed.

## 2026-08-10 (Mon) — v4.57.0 released (the expr transpiler release)

Cut v4.57.0: expr transpiler phases 1–4 + the two pre-release fixes
(generate-ssql +if round-trip port; duplicate field+op flag naming).
Fresh gate run before tagging (equivalence + all package tests, green).

Standard flow: bump ae92fa6 → bake 24c7b2f → tag v4.57.0 → push.
Proxy-verify clean on attempt 1 (GOPROXY=direct, log redirected per the
W30 lesson — no pipe on go install): binary reports v4.57.0/ae92fa67.

Post-release checklist, all done:
- debs baked + committed (82d76b4); install-local: ssql AND ssql_gpu
  both v4.57.0/82d76b45, no drift.
- Playground WASM rebuilt + gh-pages deployed (dd06530).
- WebVM: linux/386 slim cross-compiled, ssql-terminal e8a3377 pushed,
  Deploy workflow SUCCESS.
- goreleaser: SUCCESS on the tag; homebrew-ssql Casks/ssql.rb
  auto-updated to 4.57.0 (GH_PAT still proven).
- Doc audit: doc-check 36/36 green throughout; EXPRESSIONS.md
  "Performance"/"Generated code" sections were still describing the
  pre-transpiler VM world — rewritten with the v4.57 reality (native
  transpilation, tier ladder, -explain, count()-vs-len(field)
  guidance, paper pointer). README performance snippet updated
  likewise — and found 4 occurrences of a nonexistent `where -expr`
  flag (should be -if-expr), all fixed. AI-prompt CLI tests green.

Release rationale recorded: the transpiler's new public APIs (exprfn,
runtime env/coerce helpers, ssql.CompileAggExprPatched) needed a
published version before `generate go -run` can use them — that
limitation is now closed for v4.57.0 binaries.

## 2026-08-10 (Mon) — convergence Phase A: the metamorphic gate found 3 bugs

Built TestFlagExprMetamorphic per the convergence plan: 19 pairs — all
10 flag operators, negation, AND/OR clause composition, update
conditions — each asserting internal lane-consistency of BOTH the flag
pipeline and its -if-expr equivalent, plus exec(flag)==exec(expr).

First run: 7 of 19 pairs FAILED. Triage found three real, shipped bugs
(none introduced by the transpiler — all pre-dating it):

1. Record where codegen emitted gt/ge/lt/le numerically UNCONDITIONALLY
   — `-if city gt Lima` compiled to `GetOr(r,"city",float64(0)) > 0`
   and silently returned ZERO rows, while exec compares
   lexicographically. Fixed: comparison branches on the advisory field
   type when known (mirroring exec's runtime field-type branch),
   value-form heuristic otherwise. The eq/ne/gt/ge/lt/le arms also
   collapsed into one emission in the process.
2. Record update codegen (generateConditionCode) — same class, worse:
   `float64(0) > "Lima"` was a compile error. Same fix (advisory-driven
   branch).
3. Typed codegen emitted strings.Contains/HasPrefix/HasSuffix for
   contains/startswith/endswith flags WITHOUT importing "strings" —
   generated programs failed to compile in both typed lanes, from both
   where and update. Import now rides the fragment (opNeedsStrings).

Also encoded as a known gap: `-if f regex P` is a Tier-3 error in typed
codegen (gate skips those lanes) while `f matches "P"` is native —
that asymmetry is convergence unlock C.7. Two residual divergences
documented in the doc, deliberately untouched (string-field vs
numeric-looking value without advisory; int field vs float-formatted
value silently false in exec) — Phase B's shared lowering resolves
them, not quiet spot-fixes.

Test-design lesson: the first update pairs did `-set tag …` (a NEW
field) — the duckdb lane rejects conditional -set on a new field
loudly BY DESIGN, knocking the lane out for both forms. Pairs now -set
an existing field, keeping the second-engine oracle in play.

Gate hygiene: sabotaged ge→gt in the shared emission — ge_int's
go-record lane caught it. Full equivalence + generation tests +
commands + vet green; corpus/permutations run alongside.

Phase A verdict, for the convergence doc's argument: the better-than-
even odds paid out. Five independent lowerings of one semantics had
three divergences findable by the FIRST systematic diff ever run
across them.

## 2026-08-10 (Mon) — convergence Phase B: one lowering for flag conditions

condOpToExprGo (expr_cond.go) is now the single FIELD OP VALUE operator
lowering. typedWhereCondition, generateCondition (record where) and
generateConditionCode (record update) became thin wrappers: each
resolves the FIELD for its backend — struct access / advisory-typed
GetOr / heuristic GetOr, the part that legitimately differs — and
delegates the OPERATOR. The comparison emission is exprCompare,
extracted from the expression walker, so `-if pop gt X` and `pop > X`
share one implementation by construction. exec's applyOperator stays
un-converged as the differential oracle; the SQL translator stays (its
structured translation is already total).

Landed along the way (each previously a latent divergence or wart):
- regex in typed mode (unlock C.7, earlier than planned): literal
  patterns hoist a content-addressed compiled var like the expression
  form's `matches`; the metamorphic regex pair now runs ALL lanes —
  skip removed, gate validates the unlock.
- Codegen-time regex validation (invalid patterns used to panic at
  generated-program startup — or per ROW in record where).
- Record update literal regexes hoisted instead of recompiled per row.
- Unknown operators are loud codegen errors (record where's default:
  silently emitted `false` — every row dropped, no message).
- Bool-field eq/ne conditions gained typed emissions incl. the
  parameterized form; ordering ops on bool are loud.
- Record where KEEPS its runtime filter flags (-pop-gt style) — the
  parameter-reference RHS threads through the shared lowering.

Textual fallout: shared emissions parenthesize and emit numeric
literals as untyped constants — several generation-test pins updated
(one gotcha: fragment JSON encodes `>` as >, so pins around
comparison operators must use the escaped form). Semantics pinned
unchanged by the metamorphic suite throughout.

Gate hygiene: sabotaged ge→gt in the SHARED map — ge_int caught it;
one sabotage now exercises every backend at once, which is the whole
point. TestCondOpToExprGo pins per-op emissions across typed/record/
param lhs shapes + loud-error cases. Full equivalence + metamorphic +
permutations + corpus + vet green.

Net: -190 lines of duplicated operator emission replaced by one
implementation + wrappers; the class of "fixed in one lowering, still
broken in the other four" is structurally gone for flag conditions.

## 2026-08-10 (Mon) — convergence Phase C: expression canonicalization

`generate ssql` now canonicalizes trivial -if-expr predicates into
structured -if conditions as the FIRST rewrite rule
(ruleExprCanonicalize + exprToFlagConds in expr_cond.go), so simple
expressions inherit merge, range tightening, contradiction detection,
reorder, catalog extraction and join pushdown:
`where -if-expr 'pop > 9 && pop > 5'` optimizes clean through to
`-if pop gt 9`, with the rule visible under -explain.

Deliberate guard rails, each traceable to a Phase-A audit item:
- FLOAT literals refuse — `-if f gt 15.5` on an int column is silently
  false-for-every-row in exec (ParseInt fails), so canonicalizing
  `f > 15.5` would CHANGE results. The gate for this: the recognizer's
  refuse-table unit case.
- OR refuses (needs where-clause splitting — future), matches refuses
  (regex-in-shell round-trips deserve a soak), and a negated
  expression canonicalizes only as a single term (¬(a∧b) doesn't
  distribute over the flag form's AND).

Gate lesson worth remembering: the first equivalence golden used
`pop > 5 && pop >= 8`, and a gt→ge sabotage SURVIVED it — tightening
made both map to `ge 8`. Sharpened the case so the binding term sits ON
a fixture boundary (`pop > 9` vs Hanoi's pop=9); now the sabotage
visibly fails. A watch-fail that doesn't fail is telling you the case
doesn't discriminate — same moral as the corpus-fixture lesson in
CLAUDE.md, at rule granularity.

Fallout: the Phase-4-era "keeps +if-expr through reorder" pin now
canonicalizes (to `+if age gt 12` — negation survives in flag form);
split into canonicalized + non-canonicalizable (float) variants so the
original guard purpose remains covered.

Docs: EXPRESSIONS.md gained the flags-vs-expressions division-of-labour
section (completion/quoting/optimizer vs generality; canonicalization
noted). Phases A–C of the convergence are now complete: five duplicated
lowerings → one, three shipped bugs fixed, regex-in-typed unlocked,
trivial expressions optimizer-visible.

## 2026-08-10 (Mon) — v4.58.0 released (the convergence release)

Cut v4.58.0: flag/expr convergence phases A–C (metamorphic gate + the
3 bugs it caught; one shared condition lowering; regex-in-typed;
optimizer expression canonicalization) plus the EXPRESSIONS.md
division-of-labour docs.

Flow: Phase B+C commit bce7fbb (NB Phase B was never committed
separately — B and C landed together) → bump 12ca583 → bake ae2e347 →
tag v4.58.0 → push. Proxy-verify clean on attempt 1:
v4.58.0/12ca583b. Debs baked (0a7023a); install-local both binaries
v4.58.0/0a7023a7, no drift. Playground WASM → gh-pages 63603f5.
WebVM linux/386 slim → ssql-terminal 1cbe02b, Deploy triggered.
doc-check green.

Watcher gotcha worth remembering: my first goreleaser wait matched the
PREVIOUS release's completed run (filtered by name+status only) and
reported an empty conclusion with a stale cask — poll the WORKFLOW's
latest run by status instead, then read its conclusion.

Resolved before wrap-up: goreleaser SUCCESS on the v4.58.0 tag,
Casks/ssql.rb auto-updated to 4.58.0, WebVM Deploy SUCCESS. All
checklist items complete.

## 2026-08-11 (Tue) — catalog +if, and the range-extraction leak the rig caught

Ross corrected me: the SSH test rig EXISTS (LXD containers
ssql-node1/2/3, doc/research/ssh-test-environment.md, running right
now) — I'd wrongly deferred the catalog +if TODO as "needs a rig".
Saved to memory. Pushed the fresh binary to all three nodes
(lxc file push … /usr/bin/ssql — note sshRemoteBin uses the ABSOLUTE
/usr/bin/ssql path per the PATH-manipulation rule, so local dev boxes
without the deb can't run local-host shards; the rig is the honest
test surface).

**The big find**: while reading the extraction rule to add negation, I
noticed readCatalogMetadataColumns strips _from/_to — making RANGE
columns extraction-eligible — while extraction DELETES the row-level
where and range pruning is only conservative. Reproduced on the rig:
`from catalog | where -if date ge 2026-03-01` optimized into pruning-
only form and the straddling shard's 2026-02-15 row LEAKED. A shipped
silent-extra-rows bug in the optimizer. Fix: readCatalogMetadataColumns
now reports exact vs range columns; exact columns extract fully (the
metadata value holds for every row — and catalog enrichment adds the
column to rows, so full extraction is exactly equivalent); range
columns prune AND keep the row filter — which ruleCatalogPredicate-
Pushdown then ships shard-side (`-if date ge X -- where -if date ge X`)
— prune + remote filter, better than the pre-bug form ever was.

**The original goal** landed too: CatalogFilter.Negated end-to-end —
exec matcher (exact: exact complement; range: the conservative dual —
catalogRangeEntirely, skip only when the WHOLE range satisfies the
positive op, missing bounds keep the shard), parseCatalogFilters reads
_negated, generated-code literals emit Negated (with not- prefixed
flag names to avoid -if/+if param collisions), generate-ssql parses
and renders +if for both from-catalog and merge-catalog, and the
extraction rule now lifts negated conditions (exact → full, range →
prune+keep).

Gates: negated PruneCatalog unit table (incl. straddling-kept and
missing-bound conservative cases), TestCatalogPredicateExtraction
(pins all four extraction behaviours), TestCatalogSSHPruning — the
rig-gated integration test (SSQL_TEST_SSH_HOST=ssql-node1; graceful
skip without): leak regression vs baseline, negated-range dual,
negated-exact inversion. All green against the live rig. Generated
catalog code with Negated compiles and runs.

Also this morning: permutation pair set widened with `top` and a
tie-free `update -set-expr` stage (56 pipelines) — all green on first
run; after yesterday's churn that's a good soak signal rather than a
disappointment. Triples-behind-env remains open (next).

## 2026-08-11 (Tue) — opt-in 3-stage permutation gate

TestPipelinePermutationTriples: every ordered TRIPLE of the (now
8-stage) permutation set — ~300 pipelines × every lane — behind
SSQL_PERM_TRIPLES=1, since it costs ~3 minutes even fully
parallelized. Added to the CLAUDE.md release checklist as a pre-tag
gate rather than a per-run tax. The pair suite's hardcoded
group|limit exclusion generalized into permOrderHazard (a positional
`limit` downstream of `group` without an order-restoring sort/top is
INHERENTLY nondeterministic — nothing for the lanes to agree on),
unit-pinned in both directions since over-exclusion silently shrinks
coverage. Stage set extracted into permStages(), shared by pairs and
triples.

First full run: green in 179s, no findings — pairs + the metamorphic
suite had already wrung out the tail. Watcher note (self-inflicted,
twice today): an `until grep ok` wait on a two-command background run
matches the FIRST command's ok — count result lines, don't grep for
presence.

That completes all three quality-gate items from yesterday's plan:
pairs widened (green), catalog +if + the range-extraction leak fix
(rig-verified), triples opt-in (green).

## 2026-08-11 (Tue) — mmap CSV reader: shipped, with an honest benchmark

internal/mmap (linux/darwin syscall.Mmap + MADV_DONTDUMP; ReadFile
fallback; GC-driven unmap via runtime.AddCleanup with shard-closure
KeepAlive) wired into typed.ReadCSVParallel.

**Scope correction found by reading before wiring**: the proposal
planned ReadDelimParallel too, claiming the zero-copy aliased strings
are safe "for the duration of the iter.Seq". Wrong for materializing
pipelines: sort/group retain rows AFTER the source seq ends, and the
GC cannot trace string pointers into a mapping — unmap would dangle
them. Under os.ReadFile the same aliasing is safe because string
pointers keep the HEAP buffer alive. So: CSV converted (encoding/csv
COPIES field strings — ReuseRecord reuses only the slice), delim
deliberately left on ReadFile with the reasoning documented in the
mmap package comment.

**Benchmark (1.15 GB / 50M rows, warm cache, same generated program
built against pre-mmap worktree vs current tree, outputs verified
identical):**
- Raw slurp+scan: 1.44s → 1.22s (1.16–1.25×) — NOT the proposal's
  1.7–1.9×, which was the old workstation; the Ultra 9's memory
  bandwidth makes the kernel copy cheaper relative to fault costs.
- Full parallel pipeline: wall NEUTRAL (3.7s both) — the saving
  vanishes into the 24-thread parse.
- Peak RSS: 3.01 GB → 2.07 GB (slurp) / 2.21 GB (e2e) — **the file-
  sized heap allocation is gone**, the robust machine-independent win,
  proportionally bigger as inputs grow.

Verdict recorded in the proposal doc: shipped for the memory win;
wall-clock claim revised honestly. Phases B/C (Arrow IPC, fadvise)
remain unimplemented pending profiles. Gates: internal/mmap unit
tests (incl. GC-survival), `go test -race ./typed/...` (the hard gate
for stream.go), full corpus + equivalence green, pre/post outputs
byte-identical.

## 2026-08-11 (Tue) — v4.59.0 released

Cut v4.59.0: the catalog correctness release (range-extraction leak
fix + +if pruning flags), the mmap CSV reader (memory win), and the
quality-gate widening (permutation pairs + opt-in triples, rig-gated
catalog integration test).

First release to run the NEW pre-tag gate from the checklist:
SSQL_PERM_TRIPLES=1 triples + pairs + metamorphic, re-run AFTER the
mmap change since it touches every parallel lane — green in 232s.

Standard flow clean end to end: bump a7aa9da → bake 33a7639 → tag →
push; proxy verify attempt 1 (v4.59.0/a7aa9da9); debs 3e5eebb,
install-local both binaries v4.59.0 no drift; playground gh-pages
e2cace5; WebVM 951eda4 + Deploy SUCCESS; goreleaser SUCCESS; cask
auto-updated to 4.59.0; doc-check green.

## 2026-08-11 (Tue) — docsearch: hybrid RAG over the research corpus

Built scripts/docsearch (own go.mod, wrapper scripts/docsearch.sh): a
hybrid search over doc/, claude/, journal/ and root markdown — 5,400+
heading-level chunks from ~66k lines. Design sized to the corpus, not
to fashion: BM25 (pure Go, ~30 lines, no persisted index — re-chunking
takes <100ms per query) fused via Reciprocal Rank Fusion with
nomic-embed-text vectors from the local ollama (which was already
pulled). NO vector database — brute-force cosine over 5k vectors is
microseconds. Embeddings cached in .docsearch-cache.jsonl (gitignored,
44MB) keyed by chunk hash: the 2m41s embed cost is one-time; doc edits
re-embed only changed chunks; warm queries ~1.4s. Degrades to
BM25-only with a note when ollama is down (-lexical forces it).

Gotchas: (1) ollama returns EMPTY embeddings for inputs past the
model's context — one oversized chunk aborted the whole first pass;
now inputs truncate to 6k chars (retrieval only needs a section's
opening) and per-chunk failures skip with a count instead of killing
the layer. (2) The nested go.mod means `go run ./scripts/docsearch`
fails from the root module — hence the wrapper using `go -C`.

Proof the hybrid layer earns its keep — concept query "avoid copying
the whole file into memory" (no shared keywords with the answer):
lexical missed them; hybrid ranked the W18 "I/O ceiling" journal
entry, the constant-memory duckdb comparison, and the concurrency
slice-partitioning rule alongside the mmap doc. Tokenizer splits code
identifiers (exprToGo → expr/to/go) so both prose and symbol queries
hit. CLAUDE.md now tells sessions to run docsearch BEFORE designing
anything that smells previously-solved.
