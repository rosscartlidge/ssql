# Journal — 2026-W30

## 2026-07-25 (Sat) — v4.56.1 released (negation + expr-loudness patch)

Cut **v4.56.1** shipping the pre-transpiler bug sweep committed earlier
(2d890de): +if/+if-expr negation fixed in all five broken paths via the
shared parseExprConds helper; update -if-expr-only no longer unconditional
(+ the never-closed if block); -set-expr eval errors fail generated
pipelines loudly; ExprAgg/StreamExprAgg panic on non-numeric results.

Standard patch flow: version bump 0e320bd → bake 54be77d → tag v4.56.1 →
push. Proxy-warm-verify: first attempt hit a transient sum.golang.org 500
AND exposed a bug in my verify loop (`go install | tail` — the pipe masked
go install's exit status, and the "OK" then printed a STALE v4.56.0 binary
from the previous release's proxytest dir; nearly called a failed verify
good). Re-ran with the log redirected instead of piped: clean on attempt 1,
binary reports v4.56.1/0e320bdc. Lesson for the release scripts: never put
go install on the left of a pipe when its exit status is the verdict.

Debs swapped (ad522c4); install-local ssql + ssql_gpu both v4.56.1
(ad522c43, no drift). Deployed playground WASM (gh-pages bd831c5) and
WebVM binary (ssql-terminal b480339 + Deploy triggered) — patch precedent
allows skipping, but this patch changes interpreter behaviour too
(update +if-expr), so both went out (v4.50.1 precedent). doc-check 36/36.

The tag also fired goreleaser — the FIRST live test of the regenerated
GH_PAT (the v4.56.0 tap push failed 401 and was done by hand).

Status at journal time: WebVM Deploy completed SUCCESS. goreleaser still
in_progress — after it finishes, check (a) run conclusion, (b) that
Casks/ssql.rb in homebrew-ssql updated to 4.56.1 AUTOMATICALLY (proves the
regenerated GH_PAT; if it 401'd again, update the cask by hand from the
release checksums.txt as done for v4.56.0).
