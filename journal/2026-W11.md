# Week 11, 2026

## 2026-03-10 (Monday)

### Distributed Shard Catalog Design

Created `doc/research/distributed-shard-catalog.md` — design for discovering and locating shards of a dataset across multiple files and machines accessible via SSH.

**Key design decisions:**
- Catalog is a CSV file mapping shards to `host:path` with optional range metadata for partition pruning
- Range columns (`date_from`/`date_to`) and exact-value columns (`region`, `country`) enable skipping irrelevant shards before any SSH connections
- Four distribution patterns: single catalog file, per-machine catalogs merged at query time, shared location (git/NFS), auto-discovery via SSH glob
- Ties into existing `merge` command for ordered output across shards (`--merge-sort`)
- Push-down via `--remote` for map-reduce style two-level aggregation

**Usage:**
```bash
ssql from --catalog shards.csv -where date ge 2025-03-01 | ssql to table
```

**Files created:**
- `doc/research/distributed-shard-catalog.md`

### GitHub Setup

- Verified `gh` CLI authentication working
- Updated repo description to "SQL-style stream processing for the command line and Go"

### Refactor `from` into Subcommands

Mirrored the `to` subcommand pattern: `from csv`, `from tsv`, `from json`, `from jsonl`, `from arrow`, `from wav`, `from xlsx`, `from command`.

**Key changes:**
- Each format gets its own subcommand with focused flags (e.g., `-channel` only on `from wav`, `-sheet` only on `from xlsx`)
- Bare `from FILE` still works — infers format from extension, delegates to format handler
- Old `from -- cmd args` syntax removed — use `from command --` instead
- Shared execution functions (`executeFromCSV`, `executeFromTSV`, etc.) used by both subcommand handlers and bare form
- Field caching extracted into `wrapWithFieldCaching` helper
- Code generation works for all paths

**Files modified:**
- `cmd/ssql/commands/from.go` — complete rewrite with subcommand registration

**Design docs updated:**
- `doc/research/from-subcommands.md` — marked as implemented
- `doc/research/distributed-ssh-processing.md` — updated all `ssh://` URLs to `from ssh host /path` subcommand syntax, `--remote`/`--gpu` to `-remote`/`-gpu`
- `doc/research/distributed-shard-catalog.md` — updated `from -catalog` to `from catalog` subcommand syntax

**All tests passing.** Both bare `ssql from data.csv` and explicit `ssql from csv data.csv` produce identical output.

### Version v4.27.0 Released

- Bumped `cmd/ssql/version/version.txt` to `4.27.0`
- Tagged `v4.27.0` with `from` subcommand refactor

## 2026-03-11 (Tuesday)

### SSH Test Environment Setup

Set up 3 LXD containers for testing future `from ssh` and `from catalog` commands.

**Environment:**

| Node | IP | Data |
|------|-----|------|
| ssql-node1 | 10.131.56.11 | `/data/events/2025-01.csv` + `/data/services.csv` |
| ssql-node2 | 10.131.56.12 | `/data/events/2025-02.csv` |
| ssql-node3 | 10.131.56.13 | `/data/events/2025-03.csv` |

**Setup completed:**
- 3 Ubuntu 24.04 LXD containers with ssql v4.27.0
- SSH key auth configured (root user)
- SSH aliases in `~/.ssh/config` with connection multiplexing
- Test data distributed: 6 event rows per shard (Jan/Feb/Mar 2025)
- Services lookup table on node1 for join testing
- Test catalog CSV at `test-data/test-catalog.csv`
- Static IPs via netplan (DHCP was not assigning IPv4 — bypassed with static config)

**Issue encountered:** LXD containers got IPv6 but not IPv4 from DHCP despite dnsmasq running on lxdbr0. Fixed by assigning static IPs (10.131.56.11-13) and writing netplan configs to persist across restarts.

**Files created:**
- `doc/research/ssh-test-environment.md` — setup guide
- `test-data/test-catalog.csv` — shard catalog for testing
- `~/.ssh/config` — SSH aliases for ssql-node1/2/3
