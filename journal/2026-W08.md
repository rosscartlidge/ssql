# Week 8, 2026

## 2026-02-17 (Monday)

### WASM Module Redesign: TinyGo + Decoupled Architecture — v4.17.0

Rewrote `cmd/ssql-wasm` to be fully self-contained with zero dependency on `ssql/v4`. The old module imported the full ssql library (Arrow, Excel, expr-lang, etc.) just for 6 simple operations, producing a 15MB WASM binary. The new module has its own JSON parser, dataset types, and operations.

**Binary size results:**

| Build | Size |
|-------|------|
| Old (ssql/v4 dependency) | 15MB |
| Standard Go (decoupled) | 2.6MB |
| TinyGo | 245KB |

**New files:**
- `cmd/ssql-wasm/dataset.go` — Core types: `schema`, `record`, `dataset`, `rawJSON`
- `cmd/ssql-wasm/json.go` — Manual recursive-descent JSON parser & serializer (no `encoding/json`)
- `cmd/ssql-wasm/compare.go` — Comparison operators ported from old operators.go
- `cmd/ssql-wasm/main_stub.go` — Empty `main()` for non-WASM `go test` / `go build`
- `cmd/ssql-wasm/json_test.go` — 23 parser/serializer tests
- `cmd/ssql-wasm/operators_test.go` — 21 operation tests

**Rewritten files:**
- `cmd/ssql-wasm/operators.go` — Self-contained dataset methods (was ssql.Where/SortBy/etc wrappers)
- `cmd/ssql-wasm/main.go` — Uses `parseJSONArray`/`toJSON` instead of ssql bridge

**Deleted files:**
- `cmd/ssql-wasm/bridge.go` — Replaced by json.go

**Modified files:**
- `Makefile` — `wasm` target now uses TinyGo; added `wasm-go` fallback for standard Go

**Bug fixed:** Old `bridge.go` fed `JSON.stringify` output (a JSON array `[{...},{...}]`) to `ReadJSONFastFromReader` which is a JSONL parser expecting one object per line. This likely returned empty results, with the explorer silently falling back to JS aggregation.

**Design decisions:**
- No `encoding/json` — enables TinyGo which doesn't support reflection-based JSON
- `regex` operator falls back to `contains` in TinyGo (no `regexp` support)
- `sort.SliceStable` used instead of `slices.SortedFunc` (TinyGo compatible)
- JSON parser follows same recursive-descent patterns as `core.go:ParseJSONLine`
- Schema expansion handles objects with different fields (not just first-object schema)

**Commits:**
- `347398d` — feat: decouple WASM module from ssql/v4 for TinyGo builds
- `4506d46` — Bump version to v4.17.0

**44 tests passing.** JS API (`ssql-wasm.js`) unchanged — no browser-side changes needed.

**Browser verification:** Built TinyGo WASM (245KB), generated explorer with `ssql from test-data/employees.csv | ssql to explore -wasm cmd/ssql-wasm/ssql.wasm`, served via `python3 -m http.server`. Green WASM badge appeared in header, confirming the module loaded and initialized successfully in the browser.

### Advanced WASM Processing: Pipeline Builder + New Operations

Transformed the explorer from a view-only tool with a single-step aggregation panel into an interactive data workbench. The existing WASM module had 6 operations but only `groupBy` was exposed in the UI. Now all operations are accessible through a pipeline builder, plus 4 new operations.

**Phase 1 — Pipeline Builder UI** (chart.go):
- Replaced the single "Aggregation" panel (group-by + apply/reset) with a general "Pipeline" panel
- Users add steps via a dropdown: Where, Sort, Group By, Multi-Group, Distinct, Limit, Computed Column, Pivot
- Each step type renders type-specific config fields (field dropdowns, operator selects, text inputs)
- "Run Pipeline" sends all steps to `ssqlWasm.pipeline()` as a single WASM call
- Result badge shows row count: "5 rows → 3 rows"
- JS fallback still works for single group-by without WASM

**Phase 2 — Multi-Group + Multiple Aggregations** (operators.go, json.go):
- New `groupByMulti(groupFields, aggs)` method groups by composite key (multiple fields)
- Each `aggSpec` has field, func, and alias — multiple aggregations per group
- UI allows adding/removing group fields and aggregation rows dynamically
- Example: group by dept+region with sum(salary), avg(salary), count

**Phase 3 — Computed Columns** (new: expr.go):
- Minimal recursive-descent arithmetic expression evaluator (~210 lines)
- Supports: field references, numeric literals, +, -, *, /, parentheses, unary negation
- Standard operator precedence (* / before + -)
- TinyGo-compatible (no reflection, no regexp)
- New `compute(name, expr)` method adds derived fields to dataset
- Example: `salary * 12` creates an `annual_salary` column

**Phase 4 — Pivot Tables** (operators.go):
- New `pivot(rowField, colField, valField, aggFunc)` method
- Unique values of `colField` become columns in the output
- Groups by `rowField`, applies `aggFunc` to `valField` for each cell
- Missing cells filled with 0 (int64 for count, float64 for others)
- Example: pivot dept × quarter with sum(revenue) → cross-tab

**Phase 5 — WASM-backed AG-Grid** (chart.go):
- AG-Grid `onFilterChanged`/`onSortChanged` events intercepted when WASM is available
- Filter model converted to WASM `where` ops (equals→eq, greaterThan→gt, etc.)
- Sort model converted to WASM `sort` ops
- Grid ops prepended to user's pipeline steps and executed as single WASM call
- `suppressGridEvents` ref prevents re-entrant updates

**New files:**
- `cmd/ssql-wasm/expr.go` — Arithmetic expression parser + evaluator
- `cmd/ssql-wasm/expr_test.go` — 14 expression tests

**Modified files:**
- `chart.go` — Pipeline builder UI, AG-Grid WASM integration (replaced aggregation panel)
- `cmd/ssql-wasm/operators.go` — Added `groupByMulti`, `compute`, `pivot` + pipeline wiring
- `cmd/ssql-wasm/json.go` — Extended `pipelineOp` with new fields, added `aggSpec` type, `parseStringArray`, `parseAggSpecs`
- `cmd/ssql-wasm/operators_test.go` — 13 new tests (multi-group, compute, pivot, pipeline integration)
- `cmd/ssql-wasm/json_test.go` — 1 new test for parsing new op types

**Test results:** 79 tests passing (was 44). Browser-verified: WASM loads, all 5 pipeline operation types produce correct results including chained pipelines (compute→where→multi-group→sort).

**Commits:**
- `f1a5a79` — feat: add pipeline builder, multi-group, computed columns, pivot, and WASM-backed grid to explorer

## 2026-02-18 (Tuesday)

### Embed WASM Binary in Explorer HTML

Made the explorer a single self-contained HTML file when `-wasm` is used. Previously the WASM binary was copied alongside the HTML as a separate `ssql.wasm` file. Now it's base64-encoded and embedded directly in the HTML (245KB raw → ~362KB base64, total HTML ~439KB).

**Changes:**
- `chart.go` — Added `WasmBinary` field to `ExploreConfig` and template data; replaced `fetch('./ssql.wasm')` with base64 decode + `initFromBytes()`; removed `CopyExploreWasmFile` function and unused `path/filepath` import
- `cmd/ssql/commands/to.go` — Read WASM binary with `os.ReadFile`, base64-encode into `config.WasmBinary`; removed `CopyExploreWasmFile` call
- `cmd/ssql-wasm/js/ssql-wasm.js` — Added `initFromBytes(wasmBytes)` method using `WebAssembly.instantiate` (raw bytes, not streaming fetch)
- `doc/api-reference.md` — Removed `CopyExploreWasmFile` documentation
- `scripts/doc-test.sh` — Removed `CopyExploreWasmFile` from excluded functions list

**Commits:**
- `9c6d8c8` — feat: embed WASM binary in explorer HTML for single-file output

### Pivot CLI Command

Added `ssql.Pivot()` to the core library and a `pivot` CLI command. Cross-tabulates data: unique values of a column field become output columns, with cells aggregated by row field.

**Changes:**
- `sql.go` — New `Pivot(rowField, colField, valField, aggFunc)` function with count/sum/avg/min/max support, zero-filled missing cells, first-seen ordering
- `sql_test.go` — 5 pivot tests (basic sum, count, missing cells, order preservation, empty input)
- `cmd/ssql/commands/pivot.go` — New CLI command with `-row`, `-col`, `-val`, `-func` flags and `-generate` support
- `cmd/ssql/main.go` — Register pivot command
- `cmd/ssql/generation_test.go` — Pivot generation test

**Commits:**
- `c87ff71` — feat: add pivot command for cross-tabulation

### Release v4.18.0

Tagged and released v4.18.0 with embedded WASM and pivot command.

**Commits:**
- `ea78476` — journal: add WASM embedding and pivot command entries
- `1a902b5` — Bump version to v4.18.0
- `b43b4e6` — release: add ssql v4.18.0 debian packages

**Verified:** `GOPROXY=direct go install github.com/rosscartlidge/ssql/v4/cmd/ssql@v4.18.0` — installs and reports `ssql v4.18.0` correctly.

## 2026-02-22 (Sunday)

### Fold `to heatmap` into `to chart -type heatmap`

Consolidated the standalone `to heatmap` subcommand into `to chart` by adding three heatmap-specific flags (`-zmin`, `-zmax`, `-log-freq`) and routing `chartType == "heatmap"` to `HeatmapChart()` instead of `EnhancedChart()`. Removes a user decision point — heatmaps are now just another chart type.

**Changes:**
- `cmd/ssql/commands/to.go` — Removed `registerToHeatmap` and `generateToHeatmapCode` (~230 lines deleted); added `-zmin`, `-zmax`, `-log-freq` flags to `registerToChart`; handler routes heatmap to `HeatmapChart()`; code generation emits `DefaultHeatmapConfig()` + `HeatmapChart()` for heatmap type
- `cmd/ssql/generation_test.go` — Updated heatmap test: `to heatmap` → `to chart -type heatmap`
- `doc/cli-codelab.md` — Merged heatmap into chart listing

**Net result:** -109 lines. Go library API (`HeatmapChart`, `HeatmapConfig`) unchanged.

**Commits:**
- `a470120` — refactor: fold `to heatmap` into `to chart -type heatmap`

### Fix Heatmap Rendering Bugs

Testing the consolidated heatmap command revealed several pre-existing bugs in `HeatmapChart()`:

1. **ZMin/ZMax rendered as strings** — `html/template` auto-escapes `float64` values in JS `<script>` context, wrapping them in quotes. `initialZMin.toFixed()` then fails because it's a string. Fix: pass as pre-formatted strings via `%g`, use `parseFloat()` in JS.
2. **1x1 cell sizing** — Numeric axis labels (e.g. `217`, `111`) caused Plotly to treat axes as continuous, placing cells at their numeric positions with gaps. Fix: use `type: 'category'` for non-log axes.
3. **Log scale with string labels** — Y labels were always strings, breaking log scale hover. Fix: emit numeric Y array when all values parse as numbers.
4. **Unsorted Y values** — Grid rows in arbitrary insertion order caused misaligned cells on log scale. Fix: sort Y values numerically and reorder grid rows.

**Files modified:**
- `chart.go` — ZMin/ZMax as `template.JS` → pre-formatted strings; category axis default; numeric Y labels with sorting

**Commits:**
- `49476d1` — fix: heatmap template rendering and axis handling
