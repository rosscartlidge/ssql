# Week 9, 2026

## 2026-02-25 (Tuesday)

### Window Functions Command

Added SQL-style window functions as a new `window` CLI command and `Window()` library function. Window functions compute rankings, offsets, and aggregates **without collapsing rows** — every input row comes out enriched. This was ssql's biggest analytical gap vs DuckDB.

**Library additions (`sql.go`, ~300 lines):**
- `WindowConfig`, `WindowSpec`, `WindowFrame`, `OrderField` types
- `WindowFunc` sealed interface with 15 concrete types + constructors:
  - Ranking: `WRowNumber()`, `WRank()`, `WDenseRank()`, `WNtile(n)`, `WPercentRank()`
  - Offset: `WLag(field, n)`, `WLead(field, n)`, `WFirst(field)`, `WLast(field)`
  - Aggregate: `WSum(field)`, `WAvg(field)`, `WCount()`, `WMin(field)`, `WMax(field)`
- `CompareAny(a, b)` — cross-type value comparison (nil < numeric < string)
- `CompareRecordFields(a, b, orderBy)` — multi-field record comparison
- `Window([]WindowConfig) Filter[Record, Record]` — main function, supports multiple configs (clauses)

**Algorithm:** Materialize → partition → sort → compute window functions per row → emit with original fields + computed fields. Supports ROWS frame specs (UNBOUNDED PRECEDING/FOLLOWING or fixed offsets).

**Tests (`sql_test.go`, ~250 lines):**
17 tests: row_number, partitioned row_number, rank (with ties), dense_rank, lag, lead, running sum, moving average, count, min/max, multiple configs (clauses), multi-field order, empty input, CompareAny, ntile, percent_rank.

**CLI command (`cmd/ssql/commands/window.go`, ~640 lines):**
- Clause-based design: `+` separator for different window specs
- All 15 window functions as flags with proper arg parsing
- `-partition`, `-order`, `-desc`, `-rows` for window specification
- `-generate` support for code generation
- Schema propagation (adds result fields with inferred types)

**Registration and generation tests:**
- `cmd/ssql/main.go` — registered `RegisterWindow`
- `cmd/ssql/generation_test.go` — 2 generation tests (row-number, partition+sum)

**Example usage:**
```bash
# Row numbering per department
ssql from data.csv | ssql window -row-number rn -partition dept -order salary -desc

# Running total
ssql from sales.csv | ssql window -sum revenue running_total -partition dept -order date

# Top 3 per department
ssql from data.csv | ssql window -row-number rn -partition dept -order salary -desc \
  | ssql where -where rn le 3 | ssql exclude rn

# Month-over-month change
ssql from monthly.csv | ssql window -lag revenue 1 prev_rev -order month

# 3-row moving average
ssql from prices.csv | ssql window -avg price ma3 -order date -rows 2,0

# Different windows via clauses
ssql from data.csv | ssql window \
  -rank salary_rank -partition dept -order salary -desc \
  + \
  -lag date 1 prev_date -order date
```

**Implementation notes:**
- Autocli stores single-arg accumulated flags as `[]any{string}`, not `[]any{map[string]any}` — had to handle both cases in `parseSingleArgSpecs`
- Code generation uses `fmt.Sprintf("%T", fn)` to recover concrete type names from unexported WindowFunc types for emitting constructor calls
- Output order: first config's partition+order order when -order is specified, original input order otherwise

**Files created:**
- `cmd/ssql/commands/window.go`
- `journal/2026-W09.md`

**Files modified:**
- `sql.go` — Window types, functions, CompareAny, Window() implementation
- `sql_test.go` — 17 window function tests
- `cmd/ssql/main.go` — RegisterWindow
- `cmd/ssql/generation_test.go` — 2 window generation tests

**All tests passing:** `go test ./...` (except known examples dir build issue)

### Documentation and Release v4.20.0

Updated all relevant documentation for the window command:
- `doc/api-reference.md` — full "SQL Window / Analytic Functions" section with types, constructors, 3 examples; renamed old section to "Batch Window Operations"
- `doc/cli-codelab.md` — "Window Functions" tutorial (5 subsections), added to command list and Key Features
- `doc/ai-code-generation.md` — updated core operations, 5 new pattern recognition entries (#22-#26)
- `README.md` — added window CLI example to showcase section

**Version bump:** 4.19.0 → 4.20.0

**Debian packages:** Built via `make deb`, removed old 4.16.0/4.18.0 packages, added 4.20.0.

**Commits:**
- `ddd7948` — feat: add window command for SQL-style window functions
- `c25f914` — docs: add window command to tutorials and API reference
- `fdc3cf5` — Bump version to v4.20.0
- `7aaaf82` — release: add ssql v4.20.0 debian packages

**Tag:** `v4.20.0` — pushed and verified with `GOPROXY=direct go install`

### Expression: replaceRegex + v4.21.0

Added `replaceRegex(str, pattern, replacement)` to the expression language. Uses Go's `regexp.ReplaceAllString` under the hood, so full `$1`/`${name}` capture group expansion works. Returns original string on invalid regex.

**Files created:**
- `cmd/ssql/lib/runtime/runtime_test.go` — 13 tests (8 for replaceRegex, 5 for existing custom functions has/getOr/sha256)

**Files modified:**
- `cmd/ssql/lib/runtime/runtime.go` — added `replaceRegex` function, registered in all 3 env-building locations
- `cmd/ssql/commands/functions.go` — added replace/replaceRegex to string category; added signatures to all categories in summary view
- `doc/EXPRESSIONS.md` — added replace/replaceRegex to string functions table with examples

**Commits:**
- `d9ca298` — feat: add replaceRegex expression function
- `08b4fcb` — Bump version to v4.21.0

**Debian packages:** Built via `make deb`, removed 4.20.0 packages, added 4.21.0.

**All commits:**
- `d9ca298` — feat: add replaceRegex expression function
- `08b4fcb` — Bump version to v4.21.0
- `1185c17` — release: add ssql v4.21.0 debian packages

**Tag:** `v4.21.0` — pushed and verified with `GOPROXY=direct go install`

### Document all expr-lang builtins

Audited expr-lang's full builtin list (~71 functions) against what `ssql functions` documented (22). Verified all builtins work in ssql's expression environment, then updated both `cmd/ssql/commands/functions.go` and `doc/EXPRESSIONS.md`.

**Changes:**
- String: added `trimPrefix`, `trimSuffix`, `splitAfter`, `repeat`, `indexOf`, `lastIndexOf`, `hasPrefix`, `hasSuffix` (+6)
- Array: added `first`, `last`, `take`, `reverse`, `sort`, `sortBy`, `uniq`, `flatten`, `concat`, `find`, `findIndex`, `findLast`, `findLastIndex`, `groupBy`, `one`, `none`, `reduce`, `mean`, `median` (+14, reorganized into subcategories)
- New categories: Date (4: `now`, `date`, `duration`, `timezone`), Map (3: `keys`, `values`, `get`), Bitwise (8), Type/Encoding expanded (7: `type`, `toJSON`, `fromJSON`, `toBase64`, `fromBase64`, `toPairs`, `fromPairs`)
- Operators: added `contains`/`startsWith`/`endsWith` as operators, `..` range operator
- Examples section: added array operations and encoding patterns
- Total: 31 → 73 documented functions

**Commit:** `9e1f59e` — docs: document all expr-lang builtins in functions command and EXPRESSIONS.md

## 2026-02-26 (Wednesday)

### WASM Explore: Merge Group By, Add Window Functions, Embed WASM

Major overhaul of the `to explore` WASM module and UI.

**Part 1 — Unified Group By:**
- Merged `group_by` (single field/agg) and `group_by_multi` (multiple fields/aggs) into a single `group_by` that always supports 1+ fields and 1+ aggregations
- Removed "Multi-Group" from the step type dropdown
- Updated all 5 WASM files + chart.go (8 locations in the explore UI)

**Part 2 — Window Functions in Explore:**
- Ported window function algorithm from `sql.go` to the self-contained WASM module
- 15 window functions: row_number, rank, dense_rank, ntile, percent_rank, lag, lead, first, last, sum, avg, count, min, max
- Frame-aware aggregates (UNBOUNDED PRECEDING/FOLLOWING, fixed offsets)
- Full UI: partition fields, order fields (asc/desc), function list with +/- buttons, frame input
- 7 new tests (row_number, partitioned, rank, lag, running sum, moving avg, pipeline)

**Part 3 — Embedded WASM Binary:**
- WASM binary (TinyGo, 278K) now embedded in ssql CLI via `//go:embed`
- New package `cmd/ssql/wasm/` with `embed.go` + copies of ssql.wasm, wasm_exec.js, ssql-wasm.js
- `-wasm` flag changed from string (path) to bool — no external files needed
- `make wasm` now copies files to both `cmd/ssql-wasm/` and `cmd/ssql/wasm/`

**Part 4 — ag-grid v35 + Fixes:**
- Upgraded from legacy CSS themes to v35 Theming API (pinned at 35.1.0)
- Fixed deprecated `rowSelection: 'multiple'` → `{ mode: 'multiRow' }`
- Removed deprecated `animateRows`
- Fixed WASM badge race condition (async WASM init vs React render timing)

**Bug found:** `wasm_exec.js` was TinyGo version but WASM binary had been rebuilt with standard Go — runtime mismatch caused silent WASM load failure. Fixed by restoring TinyGo build + matching wasm_exec.js. Added WASM section to CLAUDE.md documenting TinyGo requirement.

**Files created:**
- `cmd/ssql/wasm/embed.go`
- `cmd/ssql/wasm/ssql.wasm`, `ssql-wasm.js`, `wasm_exec.js`

**Files modified:**
- `cmd/ssql-wasm/operators.go` — merged groupBy, added window types + method (~250 lines)
- `cmd/ssql-wasm/json.go` — removed old group_by fields, added window config parsing
- `cmd/ssql-wasm/main.go` — removed jsGroupBy
- `cmd/ssql-wasm/operators_test.go` — updated group_by tests, added 7 window tests
- `cmd/ssql-wasm/json_test.go` — updated parsing tests
- `cmd/ssql-wasm/ssql.wasm` — rebuilt with TinyGo
- `cmd/ssql/commands/to.go` — embedded WASM, `-wasm` bool flag
- `chart.go` — merged group_by UI, added window step, ag-grid v35, badge fix
- `Makefile` — `make wasm` copies to embed dir
- `CLAUDE.md` — added WASM Explore Module section
- `doc/cli-codelab.md` — updated `-wasm` usage

**Commits:**
- `d1f74ae` — feat: merge group-by, add window functions to explore, embed WASM
- `9e9c2c1` — Bump version to v4.22.0
- `0c96006` — docs: update cli-codelab for embedded WASM flag

**Tag:** `v4.22.0` — pushed to GitHub

## 2026-02-27 (Thursday)

### Streaming Optimizations: top, presorted group-by, streaming to-table

Implemented the top 3 optimizations from `doc/research/streaming-vs-materialization.md`.

**Feature 1: `top` command (heap-based top-K)**

Library (`operations.go`):
- `TopBy[T, K](n, keyFn)` — min-heap of size N, returns top N descending
- `BottomBy[T, K](n, keyFn)` — max-heap of size N, returns bottom N ascending
- Heap types: `topMinHeap`, `topMaxHeap` with `topHeapEntry` (unexported)
- O(N·log(K)) time, O(K) memory

CLI (`cmd/ssql/commands/top.go`):
- `ssql top N -field FIELD [-asc] [-generate]`
- Registered in `main.go` after `RegisterSort`

Tests: 6 library tests + 2 generation tests.

**Feature 2: Streaming group-by (`-presorted`)**

Library (`sql.go`):
- `StreamGroupByFields(sequenceField, fields...)` — tracks current key, buffers one group, emits on key change
- Output identical to `GroupByFields` — `Aggregate()` works unchanged downstream

CLI (`cmd/ssql/commands/group_by.go`):
- `-presorted` flag added
- Rejects `-presorted` with `-rollup`/`-cube`
- Code generation uses `ssql.StreamGroupByFields` when presorted

Tests: 5 library tests + 1 generation test.

**Feature 3: Streaming `to table` (`-stream`)**

Library (`io.go`):
- `DisplayTableStreaming()` / `DisplayTableStreamingTo()` — uses `iter.Pull` for two-phase processing
- Phase 1: sample first N records for column widths; Phase 2: stream rest
- Extracted helpers: `buildColumnOrder`, `calculateColumnWidths`, `printTableHeader`, `printTableSeparator`, `printTableRow`

CLI (`cmd/ssql/commands/to.go`):
- `-stream` flag + `-sample N` flag (default 100) added to `to table`

Tests: 4 library tests.

**Documentation:**
- Updated `doc/research/streaming-vs-materialization.md` — marked 3 optimizations as DONE, updated tables

**Files created:**
- `cmd/ssql/commands/top.go`

**Files modified:**
- `operations.go` — TopBy, BottomBy, heap types
- `sql.go` — StreamGroupByFields
- `io.go` — DisplayTableStreaming, extracted helpers
- `cmd/ssql/commands/group_by.go` — -presorted flag + handler + code gen
- `cmd/ssql/commands/to.go` — -stream, -sample flags
- `cmd/ssql/main.go` — RegisterTop
- `operations_test.go` — 6 TopBy/BottomBy tests
- `sql_test.go` — 5 StreamGroupByFields tests
- `io_test.go` — 4 DisplayTableStreaming tests
- `cmd/ssql/generation_test.go` — 3 generation tests
- `doc/research/streaming-vs-materialization.md` — updated status

### Streaming Window Functions Design Report

Wrote `doc/research/streaming-window-functions.md` — deep dive on how to implement streaming window functions with bounded frames.

**Key findings:**
- 13 of 15 functions can stream (not NTILE, PERCENT_RANK — need partition size)
- Running aggregates (default frame `UNBOUNDED PRECEDING TO CURRENT ROW`) need O(1) memory — no ring buffer for SUM/AVG/COUNT/FIRST/ROW_NUMBER/RANK/DENSE_RANK
- Bounded frames use ring buffer of size `preceding + following + 1`
- Incremental algorithms: Kahan-compensated sliding sum, monotonic deque for O(1) MIN/MAX
- API: separate `StreamWindow()` function returning `(Filter, error)` — matches `StreamGroupByFields` pattern
- Safety: differential testing (StreamWindow output == Window output for sorted input)
- Two-phase plan: Phase 1 = running aggregates, Phase 2 = bounded frames

**Commit:** `691ea80` — docs: add streaming window functions design report
