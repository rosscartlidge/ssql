# Week 35 (2026-08-24 → 2026-08-30)

## 2026-08-25 (Tue) — stale-page diagnosis: version in the ready text

Ross: "I dont see the records/s when I test on this machine."
Server-side reproduction showed everything working (inputRows in the
envelope, rows/s code in the page) — the cause was staleness: a serve
process started before yesterday's install keeps its old binary, and
an old tab keeps its old JS. Rather than leave that a recurring
support question, the ready text now names its build: "Engine ready —
pipeline runs against data.jsonl (ssql v4.70.0)" (ExploreConfig
.Version set by the CLI; root package stays version-agnostic). One
glance answers "am I on the new page". Explore harness green
(SSQL_UI_READY_TEXT is used verbatim by scenarios, which now
naturally see the version).

### Addendum: from parquet FILE showed no rows/s (found by Ross)

His three-step test (csv exec 642k rows/s → typed 9.0M rows/s →
parquet typed) found the gap: headInputRows recognized bare
`from X.parquet` by extension but not the `from parquet FILE`
subcommand form — "parquet" was taken as the file, the real file as a
second positional → multi-file → omitted. Format list gains parquet;
the ext-only branch folded into one spelling-agnostic check; unit
case for the subcommand form. Verified live: inputRows 14628366 on
his exact pipeline.

### Addendum 2: the rows/s display finds its first bug same-day

Ross's three-step demo (csv exec 642k/s → csv typed 9.0M/s → parquet
typed) read 2.2M rows/s on `group-by relationship` — "worked but got
a LOT SLOWER". Not a regression: agg-less group-by (DISTINCT
semantics) emitted include+typed.Distinct as SerialOnly — 14.6M rows
through one core (6.7s) — while -count ran GroupByParallel (1.5s).
The throughput display exposed a path difference that had always
existed. Fix: typed.DistinctParallel (per-shard parallel dedupe —
the expensive hashing — then a serial merge over ≤ shards×distinct
rows; race-tested), dual-templated into BOTH the agg-less group-by
and the distinct command (same SerialOnly gap). Verified on his
exact pipeline via a locally-built generated program: 6.7s → 1.5s,
14 groups. Equivalence case groupby_no_aggs_distinct (ordered, all
lanes); corpus green. Note: served ⚡ typed heads compile against the
PUBLISHED module, so they keep the serial form until the next
release — the version-pin behavior, now twice observed.

### Addendum 3: -columns values read as files (found by Ross)

Third find of the morning, same scanner: `-columns relationship`'s
value was taken as a second file → multi-file → rows/s omitted —
right on the showcase pipeline (column-pruned parquet: 14.6M rows in
0.18s ≈ 81M rows/s once displayed). The scanner now consumes an
Accumulate flag's whole value run. The lesson accumulating across
these three: headInputRows re-parses from-args with its own little
grammar, which drifts from the real flag definitions — if it grows
again, derive the file/flag split from ONE shared table with
splitServePipeline/translateFrom instead of three hand-copies.

## 2026-08-25 (Tue) — v4.71.0 released

The morning's finds shipped same-day: DistinctParallel (agg-less
group-by/distinct parallel in typed mode), both rows/s scanner fixes,
versioned ready text. Gates first-pass (triples 178s), module verify
first-attempt (3dd8494d), goreleaser 14 assets, install-local clean
1d678cbc, playground + WebVM deployed. With this release Ross's
workspace heads read 98.2M rows/s on the column-pruned parquet
pipeline — and the agg-less form joins the parallel path.

## 2026-08-25 (Tue) — from -records: the count lives with the format

Ross, after the three scanner-drift bugs: "like completion and help
it should be built into the from command... the from command knows
all about the file so is best placed to know how to calculate the
number of lines in the most efficient way." Exactly the drift lesson,
solved structurally: `from … -records` prints ONE integer — the
records that invocation would produce — cheapest per format (parquet
footer; newline scan −header for csv/tsv; jsonl minus a _schema line;
-sample min(N,rows) parsed by from's OWN grammar; multi-file sums;
stdin/JSON-array loud refusals). serve's headInputRows collapsed from
a hand-rolled arg grammar to "exec stage0 + [-records]" — the entire
drifting-copy class is dead. Caching stayed serve-side per doctrine
(nothing caches across CLI invocations): keyed by opaque stage-0
argv, invalidated by a readdir fingerprint of the served dir's data
files (name:size:mtime) — verified live: first csv head +0.15s,
second 0.03s; limit cap still applies stage-side; pruned-parquet and
sampled paths correct. Tests: TestFromRecords CLI matrix (8 cases
incl. multi-file sum and both refusals), TestHeadInputRowsShape +
TestDirDataFingerprint replacing the deleted grammar table. Bonus
standalone feature: `ssql from big.parquet -records` answers
instantly where `| ssql count` reads everything.

## 2026-08-26 (Wed) — the consolidation trio: scale gate, parallel draws, -readonly

Pre-release batch per the plan Ross approved ("great idea - lets do
them"):

1. **Scale gate (DFC113) implemented** — cmd/ssql/scale_test.go,
   SSQL_SCALE=1 opt-in, cached ~120MB/3M-row fixture (tempdir keyed
   by generator version), nine budget cases mapping the week's trap
   inventory. First full run all-green in 15s warm; sabotage-verified
   by reintroducing the parquet schema-mode full decode (tripped at
   2.46s vs 1s). On the pre-tag checklist; CLAUDE.md gains the
   pipe-buffer doctrine line. The serve-records-cache case automates
   what was manual-only yesterday.
2. **Parallel seek draws** — sampleLineStarts runs draws in
   32-worker batches over the shared *os.File (ReadAt is
   offset-explicit, so safe); ADMISSION stays sequential by draw
   index, which reproduces the serial selection exactly — the first
   cut admitted whole batches and returned 64 rows for n=50, caught
   immediately by the determinism test. Local timing unchanged
   (31ms); the win is N×RTT/32 on FUSE mounts (DFC112 item closed).
3. **serve -readonly** — validateReadonly rejects tee, `to FMT` with
   any bare token (conservative: flag VALUES also reject — can't
   classify them without copying the command's grammar, which DFC115
   forbids; false positives are safe and the error says so), -o, and
   generate -run/-build. 403s; unit table ×8 + integration (tee
   rejected, reads fine).

### Addendum: one stale assertion

The full suite caught TestTypedDistinct asserting the generated
source contains typed.Distinct( — the planner now correctly emits
DistinctParallel for the parallel plan (output-correctness
assertions were already passing). Assertion accepts either form.
Interesting note: yesterday's DistinctParallel work ran the corpus
and equivalence gates but not this older generation-source test —
full-suite-before-commit earns its keep again.

## 2026-08-26 (Wed) — v4.72.0 released (the consolidation release)

The trio + -records shipped as planned: from -records (source-owned
counting, DFC115 principle in CLAUDE.md), the scale gate (DFC113,
implemented and sabotage-verified — debuted ON the pre-tag list this
very release, 7.1s warm), parallel seek draws, serve -readonly. One
stale-assertion fix on the way (TestTypedDistinct expected the serial
emission). Gates first-pass incl. the new scale gate; module verify
first-attempt (d84034c0); goreleaser 14 assets; install-local clean
1f728046 both binaries; playground + WebVM deployed. Queue for next:
from https:// with Range (the headline candidate), parquet value
sampler hook, optimizer-placed cuts.
